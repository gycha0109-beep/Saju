import { describe, expect, test } from 'vitest';
import {
  buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract,
  type ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeInteractionEligibilityMethodologyReviewReport,
} from '../src/index.js';

function i113(
  overrides: Partial<ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeInteractionEligibilityMethodologyReviewReport> = {},
): ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeInteractionEligibilityMethodologyReviewReport {
  const base = {
    reviewId: 'i113_i114_fixture',
    reviewVersion: 'fixture',
    status: 'RESOLVED_SOURCE_KE_INTERACTION_ELIGIBILITY_METHODOLOGY_REVIEW',
    decision: 'VISIBLE_STEM_POSITIONAL_METHODOLOGY_AVAILABLE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_GAP_REMAINS',
    upstreamI107EvidenceId: 'i107_fixture',
    upstreamI112ReviewId: 'i112_fixture',
    candidateSourceId: 'source_chenyuan_sizhu_yuce_rumen_1995_isbn9787805922515',
    directionalBindingLayerResolved: true,
    i98KeDamageVocabularyStillBlocked: true,
    visibleStemSourceLocatorVerified: true,
    visibleStemSourceAnchor: '两干相克，邻干力大，隔干次之，远干无力',
    visibleStemPositionalApplicabilityLanguageAvailable: true,
    visibleStemSourcePositionalVocabulary: ['邻干', '隔干', '远干'],
    sourceContainsQualitativePositionalForceLanguage: true,
    repositoryPillarDistanceMappingResolved: false,
    sourcePositionVocabularyMayBeDirectlyMappedToNumericDistance: false,
    visibleStemInteractionEligibilityResolved: false,
    visibleStemPositionalApplicabilityAdapterContractReady: true,
    farStemNoForceMayBePromotedToNoInteraction: false,
    qualitativePositionForceMayBeConvertedToNumericWeight: false,
    qualitativePositionForceMayResolveGlobalRelativeForce: false,
    branchHiddenStemSourceLocatorVerified: true,
    branchHiddenStemFiveElementApplicabilityAuthorityAvailable: true,
    branchHiddenStemPositionalInteractionEligibilityAuthorityAvailable: false,
    visibleStemToHiddenStemEligibilityAuthorityAvailable: false,
    hiddenStemToVisibleStemEligibilityAuthorityAvailable: false,
    hiddenStemToHiddenStemEligibilityAuthorityAvailable: false,
    hiddenStemMembershipAloneMayEstablishInteractionEligibility: false,
    hiddenStemCurrentAuthorityGap: 'SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED',
    genericAllComponentInteractionEligibilityResolved: false,
    effectiveInteractionSetResolved: false,
    currentChartInteractionEligibilityMaterializationAuthorizedByThisGate: false,
    directionalBindingMayBePromotedToEffectiveInteraction: false,
    directionalBindingMayBePromotedToDamageOutcome: false,
    directionalBindingMayBePromotedToDamageMagnitude: false,
    currentChartPillarDistanceMayBeUsedBeforeAdapterContract: false,
    rawEarthlyBranchElementMayParticipate: false,
    noTrackedRelationTouchSemanticsRemainUnchanged: true,
    structuralRelationKindMutationAuthorized: false,
    sourceActivationVerdictAuthorized: false,
    sourcePersistenceVerdictAuthorized: false,
    sourceEffectiveSupportVerdictAuthorized: false,
    relativeForceVerdictAuthorized: false,
    clashWinnerVerdictAuthorized: false,
    rescueEffectAuthorized: false,
    clashSettlementAuthorized: false,
    crossRelationPrecedenceAuthorized: false,
    targetPostRelationRootState: 'not_determined',
    effectiveMechanismForceVerdict: 'not_determined',
    relationSpecificUsefulnessHarmfulness: 'not_determined',
    i98ResearchMethodologyMaterializationAuthorized: false,
    classificationAuthorized: false,
    numericScoringAuthorized: false,
    recommendedNextGate: 'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_POSITIONAL_APPLICABILITY_ADAPTER_CONTRACT',
    notes: [],
  } as const;
  return { ...base, ...overrides } as unknown as ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeInteractionEligibilityMethodologyReviewReport;
}

describe('I114 source 克 visible-stem positional applicability adapter contract', () => {
  test('freezes the narrow visible-heavenly-stem positional topology contract', () => {
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(report.status).toBe('RESOLVED_SOURCE_KE_VISIBLE_STEM_POSITIONAL_APPLICABILITY_ADAPTER_CONTRACT');
    expect(report.decision).toBe('VISIBLE_STEM_POSITIONAL_TOPOLOGY_ADAPTER_CONTRACT_FROZEN_NO_EFFECT_OR_INTERACTION_VERDICT');
    expect(report.componentScope).toBe('VISIBLE_HEAVENLY_STEM_PAIR_ONLY');
    expect(report.repositoryPillarOrder).toEqual(['year', 'month', 'day', 'hour']);
    expect(report.positionMappingCount).toBe(3);
  });

  test('maps 邻干 隔干 远干 to deterministic slot topology only', () => {
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(report.positionMappings.map((item) => [item.sourcePositionVocabulary, item.repositorySlotDistance, item.repositoryPositionTopology])).toEqual([
      ['邻干', 1, 'ADJACENT_PILLAR_STEMS'],
      ['隔干', 2, 'ONE_INTERVENING_PILLAR_STEM'],
      ['远干', 3, 'TWO_INTERVENING_PILLAR_STEMS'],
    ]);
    expect(report.repositorySlotDistanceMappingContractFrozen).toBe(true);
    expect(report.repositorySlotDistanceMayBeDerivedByAbsolutePillarIndexDifference).toBe(true);
    expect(report.sourceVocabularyMayBeMappedToRepositorySlotDistanceByThisContract).toBe(true);
  });

  test('preserves 力大 次之 无力 as qualitative source labels rather than numeric weights', () => {
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(report.sourceQualitativeForceVocabulary).toEqual(['力大', '次之', '无力']);
    expect(report.positionMappings.every((item) => item.distanceIsTopologyOnly)).toBe(true);
    expect(report.positionMappings.every((item) => item.distanceIsForceWeight === false)).toBe(true);
    expect(report.positionMappings.every((item) => item.qualitativeForceLabelIsNumericWeight === false)).toBe(true);
    expect(report.qualitativeForceLabelsArePreservedSourceVocabularyOnly).toBe(true);
    expect(report.qualitativeForceMayBeConvertedToNumericWeight).toBe(false);
    expect(report.repositorySlotDistanceMayBeConvertedToNumericForceWeight).toBe(false);
  });

  test('requires an existing directional 克 binding and two visible stems before positional evidence', () => {
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(report.pairMustAlreadyBeExactDirectionalKeBinding).toBe(true);
    expect(report.bothParticipantsMustBeVisibleHeavenlyStems).toBe(true);
    expect(report.selfPairAllowed).toBe(false);
    expect(report.sourceTargetDirectionMustRemainFromDirectionalBinding).toBe(true);
    expect(report.positionalEvidenceAdapterImplementationAuthorized).toBe(true);
    expect(report.currentChartPositionalEvidenceMaterializedByThisGate).toBe(false);
  });

  test('keeps hidden stems and raw branch elements completely outside this adapter', () => {
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(report.hiddenStemParticipantMayParticipate).toBe(false);
    expect(report.rawEarthlyBranchElementMayParticipate).toBe(false);
    expect(report.hiddenStemInteractionEligibilityGapRemains).toBe(true);
    expect(report.hiddenStemAuthorityGap).toBe('SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED');
  });

  test('does not promote 远干无力 or any positional label to interaction damage magnitude or settlement', () => {
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(report.farStemNoForceMayBePromotedToNoInteraction).toBe(false);
    expect(report.positionalTopologyIsInteractionEligibilityVerdict).toBe(false);
    expect(report.positionalTopologyIsDamageVerdict).toBe(false);
    expect(report.positionalTopologyIsDamageMagnitude).toBe(false);
    expect(report.visibleStemEffectiveInteractionEligibilityAuthorized).toBe(false);
    expect(report.damageOutcomeAuthorized).toBe(false);
    expect(report.damageMagnitudeAuthorized).toBe(false);
    expect(report.settlementOutcomeAuthorized).toBe(false);
    expect(report.i98KeDamageVocabularyEvaluationResolved).toBe(false);
  });

  test('fails closed when I113 tries to resolve visible-stem interaction eligibility', () => {
    const invalid = i113({ visibleStemInteractionEligibilityResolved: true as false });
    const report = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(invalid);
    expect(report.status).toBe('I113_UNRESOLVED_OR_INVALID');
    expect(report.positionMappings).toEqual([]);
    expect(report.repositorySlotDistanceMappingContractFrozen).toBe(false);
    expect(report.positionalEvidenceAdapterImplementationAuthorized).toBe(false);
  });

  test('is deterministic and keeps effect settlement production and classifier guards closed', () => {
    const first = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    const second = buildI114ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemPositionalApplicabilityAdapterContract(i113());
    expect(first.contractId).toBe(second.contractId);
    expect(first.effectiveInteractionSetResolved).toBe(false);
    expect(first.i98ResearchMethodologyMaterializationAuthorized).toBe(false);
    expect(first.noTrackedRelationTouchSemanticsRemainUnchanged).toBe(true);
    expect(first.structuralRelationKindMutationAuthorized).toBe(false);
    expect(first.sourceActivationVerdictAuthorized).toBe(false);
    expect(first.sourcePersistenceVerdictAuthorized).toBe(false);
    expect(first.sourceEffectiveSupportVerdictAuthorized).toBe(false);
    expect(first.relativeForceVerdictAuthorized).toBe(false);
    expect(first.clashSettlementAuthorized).toBe(false);
    expect(first.crossRelationPrecedenceAuthorized).toBe(false);
    expect(first.classificationAuthorized).toBe(false);
    expect(first.numericScoringAuthorized).toBe(false);
    expect(first.recommendedNextGate).toBe('UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_POSITIONAL_APPLICABILITY_EVIDENCE');
  });
});
