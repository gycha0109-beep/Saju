import { describe, expect, it } from 'vitest';
import {
  buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence,
  RELATIONSHIP_SPOUSE_T8_INNERCIPHER_2026_PRODUCT_CONTRACT_DIRECT_BODY_BOUNDARY_CANDIDATE,
  RELATIONSHIP_SPOUSE_T8_INNERCIPHER_2026_PRODUCT_CONTRACT_DIRECT_BODY_BOUNDARY_CONTROL_IDS,
} from '../src/research/relationship-spouse-t8-innercipher-2026-product-contract-direct-body-boundary-evidence.js';

describe('relationship spouse T8 InnerCipher 2026 product-contract direct-body boundary evidence', () => {
  it('pins the exact public source identity and complete direct HTML traversal', () => {
    const candidate = RELATIONSHIP_SPOUSE_T8_INNERCIPHER_2026_PRODUCT_CONTRACT_DIRECT_BODY_BOUNDARY_CANDIDATE;
    expect(candidate.frontierIssue).toBe(545);
    expect(candidate.publicUrl).toBe('https://www.innerciphers.com/en/articles/spouse-palace-bazi');
    expect(candidate.directBodyAcquisition.completeDirectHtmlBodyAcquired).toBe(true);
    expect(candidate.directBodyAcquisition.fullPageDirectTraversalPerformed).toBe(true);
    expect(candidate.directBodyAcquisition.observedHtmlLineCount).toBe(146);
    expect(candidate.directBodyAcquisition.observedHtmlLineStart).toBe(0);
    expect(candidate.directBodyAcquisition.observedHtmlLineEnd).toBe(145);
  });

  it('preserves the source-authored current product-contract boundary without inventing a selector', () => {
    const report = buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence();
    expect(report.status).toBe(
      'DIRECT_BODY_CURRENT_PRODUCT_CONTRACT_REJECTS_STANDALONE_GENDERED_SPOUSE_RULES_POSITIVE_NEUTRAL_SPOUSE_PALACE_METHOD_NEGATIVE_COMPLETE_ROLE_NEUTRAL_SPOUSE_SELECTOR',
    );
    expect(report.currentExternalProductContractObserved).toBe(true);
    expect(report.standaloneGenderedSpouseRuleRejectedByProductContract).toBe(true);
    expect(report.sexCommonSpousePalaceMethodObserved).toBe(true);
    expect(report.completeRoleNeutralSpouseSelectorEstablished).toBe(false);
    expect(report.candidate.directBodyEvidence.genderSpecificRulesDescribedAsSchoolDependent).toBe(true);
    expect(report.candidate.directBodyEvidence.fixedSexCommonDayBranchHiddenStemTenGodSequencePublished).toBe(true);
    expect(report.candidate.directBodyEvidence.hiddenTenGodRolesTreatedAsStructuralNotPartnerTraits).toBe(true);
    expect(report.candidate.directBodyEvidence.fullReportUsesDayBranchHiddenTenGodsAsOneInput).toBe(true);
    expect(report.candidate.directBodyEvidence.nativeSexIndependentReplacementSpouseStarSelectorObserved).toBe(false);
    expect(report.candidate.directBodyEvidence.completeRoleNeutralSpouseSelectorObserved).toBe(false);
  });

  it('keeps all unresolved authority gaps open and Production on HOLD', () => {
    const report = buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence();
    expect(report.exactUpstreamTwoOfFiveStateAccepted).toBe(true);
    expect(report.explicitRoleNeutralNatalMappingEstablished).toBe(false);
    expect(report.currentGovernedMethodSemanticCorrespondenceEstablished).toBe(false);
    expect(report.currentRelationshipT6InputPathEstablished).toBe(false);
    expect(report.qualifyingPrimaryWitnessRemainsClosed).toBe(true);
    expect(report.independentNormativeProvenanceRemainsClosed).toBe(true);
    expect(report.authorityGapsClosedCount).toBe(2);
    expect(report.authorityGapsOpenCount).toBe(3);
    expect(report.authorityAdmissionReady).toBe(false);
    expect(report.spouseT8ProducerReady).toBe(false);
    expect(report.productionPromotionReady).toBe(false);
    expect(report.productionState).toBe('HOLD');
  });

  it('preserves no-bypass and no-stitching boundaries', () => {
    const candidate = RELATIONSHIP_SPOUSE_T8_INNERCIPHER_2026_PRODUCT_CONTRACT_DIRECT_BODY_BOUNDARY_CANDIDATE;
    expect(candidate.guessedOpaqueIdentifierCount).toBe(0);
    expect(candidate.loginBypass).toBe(false);
    expect(candidate.sessionBypass).toBe(false);
    expect(candidate.institutionAuthBypass).toBe(false);
    expect(candidate.paywallBypass).toBe(false);
    expect(candidate.drmRequestExecuted).toBe(false);
    expect(candidate.decryptionActionExecuted).toBe(false);
    expect(candidate.tlsVerificationDisabled).toBe(false);
    expect(candidate.crossSourceSemanticStitching).toBe(false);
  });

  it('publishes the complete control set and deterministic evidence id', () => {
    const a = buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence();
    const b = buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence();
    expect(a.controlIds).toEqual(RELATIONSHIP_SPOUSE_T8_INNERCIPHER_2026_PRODUCT_CONTRACT_DIRECT_BODY_BOUNDARY_CONTROL_IDS);
    expect(a.controlCount).toBe(RELATIONSHIP_SPOUSE_T8_INNERCIPHER_2026_PRODUCT_CONTRACT_DIRECT_BODY_BOUNDARY_CONTROL_IDS.length);
    expect(a.evidenceId).toBe(b.evidenceId);
    expect(a.recommendedNextAction).toBe(
      'CONTINUE_SINGLE_SOURCE_PUBLIC_EXECUTABLE_OR_COMPLETE_BODY_DISCOVERY_FOR_COMPLETE_ROLE_NEUTRAL_SPOUSE_SELECTOR',
    );
  });
});
