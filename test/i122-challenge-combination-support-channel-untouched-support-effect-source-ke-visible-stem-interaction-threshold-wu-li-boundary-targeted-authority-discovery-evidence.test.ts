import { describe, expect, test } from 'vitest';
import {
  buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence,
  type ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReviewReport,
} from '../src/index.js';

function i121(
  overrides: Record<string, unknown> = {},
): ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReviewReport {
  const base = {
    reviewId: 'i121_i122_fixture',
    reviewVersion: 'fixture',
    status:
      'RESOLVED_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_READINESS',
    decision:
      'WU_LI_BOUNDARY_TARGETED_DISCOVERY_AUTHORIZED_SINGLE_CANDIDATE_FULL_SIX_CONTRACT_PRESERVED_NO_CROSS_SOURCE_EQUIVALENCE',
    upstreamI120EvidenceId: 'i120_fixture',
    targetSourceTerm: '克',
    targetScope: 'VISIBLE_HEAVENLY_STEM_BINARY_EFFECTIVE_INTERACTION_ELIGIBILITY',
    targetRequirementId: 'WU_LI_BOUNDARY_SEMANTICS_AND_EXCEPTIONS',
    targetLiteralForms: ['无力', '無力'],
    targetSemanticQuestion: 'fixture',
    targetedDiscoveryMayProceed: true,
    discoveryMode:
      'TARGET_WU_LI_BOUNDARY_WHILE_PRESERVING_SINGLE_CANDIDATE_FULL_SIX_REQUIREMENT_ADMISSION',
    visibleStemKeContextRequired: true,
    literalWuLiOrOrthographicEquivalentRequired: true,
    explicitSemanticBoundaryStatementRequired: true,
    explicitNoInteractionMeaningIfClaimedMustBeSourceLocal: true,
    explicitNonZeroOrResidualForceMeaningIfClaimedMustBeSourceLocal: true,
    explicitExceptionConditionsRequiredIfBoundaryIsConditional: true,
    sameCandidateMustExposeAllSixI118SemanticLociBeforeRegistration: true,
    targetedRequirementHitAloneMayAuthorizeRegistration: false,
    targetedRequirementHitAloneMayAuthorizeThreshold: false,
    candidateMayProceedToCoverageEvaluationOnlyAfterFullSixLocatorAdmission: true,
    simplifiedTraditionalOrthographicEquivalenceAllowed: true,
    semanticSynonymWithoutWuLiMaySatisfyTarget: false,
    buNengXiangKeMayBeMappedToWuLiWithoutSameSourceStatement: false,
    buYiKeLunMayBeMappedToWuLiWithoutSameSourceStatement: false,
    buZuoKeLunMayBeMappedToWuLiWithoutSameSourceStatement: false,
    lingEffectMayBeMappedToWuLiWithoutSameSourceStatement: false,
    searchSnippetMayCountAsAuthorityEvidence: false,
    modelSynthesisMayCountAsAuthorityEvidence: false,
    generalKnowledgeMayCountAsAuthorityEvidence: false,
    numericCalibrationMayCountAsNormativeAuthority: false,
    crossCandidateCompositionAuthorized: false,
    partialCandidateCompositionAuthorized: false,
    existingI107WuLiTextAutomaticallyDefinesBoundary: false,
    weiQianliBuNengXiangKeAutomaticallyDefinesI107WuLi: false,
    weiQianliCandidateMayBeReinspectedForWuLiInVerifiedSameWorkText: true,
    alternateEditionOrTranscriptionTextMayBeBorrowedWithoutIdentityResolution: false,
    strongestRejectedCandidateMayBePromotedByThisGate: false,
    candidateDiscoveryPerformedByThisGate: false,
    candidateRegisteredByThisGate: false,
    requirementCoverageEvaluatedByThisGate: false,
    authorityAcquiredByThisGate: false,
    visibleStemBinaryEffectiveInteractionEligibilityResolved: false,
    effectiveInteractionSetResolved: false,
    thresholdRuleCreatedByThisGate: false,
    damageEvaluationAuthorized: false,
    i98KeDamageVocabularyEvaluationResolved: false,
    i98ResearchMethodologyMaterializationAuthorized: false,
    hiddenStemInteractionEligibilityGapRemains: true,
    hiddenStemAuthorityGap:
      'SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED',
    methodologyDefinitionCreatedByThisGate: false,
    ruleDefinitionCreatedByThisGate: false,
    registrySnapshotMutatedByThisGate: false,
    reviewAttestationCreatedByThisGate: false,
    noTrackedRelationTouchSemanticsRemainUnchanged: true,
    structuralRelationKindMutationAuthorized: false,
    sourceActivationVerdictAuthorized: false,
    sourcePersistenceVerdictAuthorized: false,
    sourceEffectiveSupportVerdictAuthorized: false,
    relativeForceVerdictAuthorized: false,
    clashWinnerVerdictAuthorized: false,
    rescueEffectAuthorized: false,
    clashSettlementAuthorized: false,
    crossRelationPrecedenceAuthorized: false,
    targetPostRelationRootState: 'not_determined',
    effectiveMechanismForceVerdict: 'not_determined',
    relationSpecificUsefulnessHarmfulness: 'not_determined',
    classificationAuthorized: false,
    numericScoringAuthorized: false,
    recommendedNextGate:
      'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_EVIDENCE',
    notes: [],
  } as const;

  return { ...base, ...overrides } as unknown as ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReviewReport;
}

describe('I122 source 克 targeted 无力 boundary authority discovery evidence', () => {
  test('records complementary but noncomposable negative discovery evidence', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );

    expect(report.status).toBe(
      'RESOLVED_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_EVIDENCE',
    );
    expect(report.decision).toBe(
      'TARGETED_WU_LI_DISCOVERY_FOUND_COMPLEMENTARY_NONCOMPOSABLE_EVIDENCE_NO_SINGLE_SOURCE_FULL_SIX_ADMISSION',
    );
    expect(report.targetedDiscoveryPerformed).toBe(true);
    expect(report.inspectedCandidateCount).toBe(4);
    expect(report.registeredCandidateCount).toBe(0);
    expect(report.fullSixCandidateCount).toBe(0);
  });

  test('preserves the existing I107 literal while refusing to infer its semantic boundary', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );
    const chen = report.inspectedCandidates.find((item) => item.sourceReference.author === '陈园');

    expect(chen?.literalWuLiObserved).toBe(true);
    expect(chen?.explicitWuLiSemanticBoundaryObserved).toBe(false);
    expect(chen?.disposition).toBe('LITERAL_PRESENT_BOUNDARY_UNDEFINED');
    expect(report.existingI107LiteralBoundaryStillUndefined).toBe(true);
    expect(report.wuLiMayBeEquatedToBuNengXiangKe).toBe(false);
  });

  test('records the verified Wei Qianli work identity without inventing a 無力 locator', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );
    const wei = report.inspectedCandidates.find((item) => item.sourceReference.author === '韋千里');

    expect(wei).toBeDefined();
    expect(wei?.sourceReference.publicationYear).toBe(1935);
    expect(wei?.sourceReference.provenanceTier).toBe('primary');
    expect(wei?.literalWuLiObserved).toBe(false);
    expect(wei?.explicitConditionalInteractionBoundaryObserved).toBe(true);
    expect(report.weiQianliSameWorkWuLiLocatorVerified).toBe(false);
  });

  test('records Zhu Zuxia conditional remote-interaction boundary but not the target literal', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );
    const zhu = report.inspectedCandidates.find((item) => item.sourceReference.author === '朱祖夏');

    expect(zhu).toBeDefined();
    expect(zhu?.literalWuLiObserved).toBe(false);
    expect(zhu?.explicitConditionalInteractionBoundaryObserved).toBe(true);
    expect(zhu?.disposition).toBe('BOUNDARY_PRESENT_LITERAL_ABSENT_OR_DIFFERENT_CONTEXT');
    expect(report.zhuZuxiaRemoteConditionalBoundaryObserved).toBe(true);
    expect(report.zhuZuxiaTargetLiteralObservedInRemoteBoundarySection).toBe(false);
  });

  test('records practitioner 无力遥克 usage without upgrading an example into a general threshold', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );
    const practitioner = report.inspectedCandidates.find(
      (item) => item.sourceReference.sourceId === 'source_yimeng_tiangan_ke_theory_2026_sina_kandian',
    );

    expect(practitioner?.literalWuLiObserved).toBe(true);
    expect(practitioner?.visibleStemKeContextObserved).toBe(true);
    expect(practitioner?.explicitWuLiSemanticBoundaryObserved).toBe(false);
    expect(report.practitionerRemoteWuLiLiteralObserved).toBe(true);
    expect(report.practitionerRemoteWuLiBoundaryExplicitlyDefined).toBe(false);
    expect(report.wuLiMayBeEquatedToWeakButConditionalInteraction).toBe(false);
  });

  test('forbids composition of complementary candidates and preserves authority guards', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );

    expect(report.complementaryEvidenceExistsAcrossCandidates).toBe(true);
    expect(report.complementaryEvidenceMayBeComposed).toBe(false);
    expect(report.crossCandidateSemanticEquivalenceAuthorized).toBe(false);
    expect(report.searchSnippetMayCountAsAuthorityEvidence).toBe(false);
    expect(report.modelSynthesisMayCountAsAuthorityEvidence).toBe(false);
    expect(report.noResultMayCreateDefaultThreshold).toBe(false);
    expect(report.authorityAcquiredByThisGate).toBe(false);
    expect(report.visibleStemBinaryEffectiveInteractionEligibilityResolved).toBe(false);
    expect(report.thresholdRuleCreatedByThisGate).toBe(false);
    expect(report.classificationAuthorized).toBe(false);
    expect(report.numericScoringAuthorized).toBe(false);
  });

  test('is deterministic and routes the complementary-evidence frontier to composition-policy readiness', () => {
    const first = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );
    const second = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121(),
    );

    expect(first.evidenceId).toBe(second.evidenceId);
    expect(first.literalBearingCandidateCount).toBe(2);
    expect(first.explicitBoundaryBearingCandidateCount).toBe(2);
    expect(first.recommendedNextGate).toBe(
      'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_CANDIDATE_SET_COMPOSITION_POLICY_READINESS_REVIEW',
    );
    expect(first.methodologyDefinitionCreatedByThisGate).toBe(false);
    expect(first.ruleDefinitionCreatedByThisGate).toBe(false);
    expect(first.registrySnapshotMutatedByThisGate).toBe(false);
  });

  test('fails closed when I121 targeted discovery readiness is no longer exact', () => {
    const report = buildI122ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryEvidence(
      i121({ crossCandidateCompositionAuthorized: true }),
    );

    expect(report.status).toBe('I121_UNRESOLVED_OR_INVALID');
    expect(report.decision).toBe('WU_LI_BOUNDARY_TARGETED_DISCOVERY_NOT_PERFORMED');
    expect(report.targetedDiscoveryPerformed).toBe(false);
    expect(report.inspectedCandidateCount).toBe(0);
    expect(report.registeredCandidateCount).toBe(0);
    expect(report.recommendedNextGate).toBe(
      'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_READINESS_REVIEW',
    );
  });
});
