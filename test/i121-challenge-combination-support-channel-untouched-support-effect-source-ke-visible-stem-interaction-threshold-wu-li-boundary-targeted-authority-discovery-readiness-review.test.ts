import { describe, expect, test } from 'vitest';
import {
  buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview,
  type ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdAuthorityCandidateDiscoveryEvidenceReport,
} from '../src/index.js';

const STRONGEST_ID = 'wei_qianli_i120_fixture';

function i120(
  overrides: Record<string, unknown> = {},
): ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdAuthorityCandidateDiscoveryEvidenceReport {
  const strongest = {
    candidateInspectionId: STRONGEST_ID,
    registrationAcceptedUnderI119: false,
    allSixRequiredSemanticLocatorsVerified: false,
    missingRequiredSemanticLocatorIds: ['WU_LI_BOUNDARY_SEMANTICS_AND_EXCEPTIONS'],
  };
  const base = {
    evidenceId: 'i120_i121_fixture',
    evidenceVersion: 'fixture',
    status:
      'RESOLVED_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_AUTHORITY_CANDIDATE_DISCOVERY_EVIDENCE',
    decision:
      'NO_SINGLE_CANDIDATE_WITH_ALL_SIX_I118_REQUIRED_SEMANTIC_LOCATORS_VERIFIED_THRESHOLD_AUTHORITY_NOT_REGISTERED',
    upstreamI119ReviewId: 'i119_fixture',
    externalDiscoveryPerformed: true,
    targetSourceTerm: '克',
    targetScope: 'VISIBLE_HEAVENLY_STEM_BINARY_EFFECTIVE_INTERACTION_ELIGIBILITY',
    discoveryMode:
      'SINGLE_CANDIDATE_FULL_SIX_REQUIREMENT_VISIBLE_STEM_THRESHOLD_AUTHORITY_ONLY',
    discoverySearchScope: [],
    inspectedCandidateCount: 1,
    inspectedCandidates: [strongest],
    registeredCandidateCount: 0,
    registeredCandidate: null,
    strongestRejectedCandidateInspectionId: STRONGEST_ID,
    strongestRejectedCandidateMissingRequirementIds: [
      'WU_LI_BOUNDARY_SEMANTICS_AND_EXCEPTIONS',
    ],
    noFullSixSemanticLocatorCandidateVerified: true,
    candidateRegistrationPerformedByThisGate: false,
    candidateRequirementCoverageEvaluatedByThisGate: false,
    candidateRegistrationMeansRequirementSatisfied: false,
    candidateSatisfiesAllSixRequirements: 'not_evaluated',
    existingI107CandidatePromoted: false,
    weiQianliCandidatePromoted: false,
    weiQianliQualitativeVsBinaryDistinctionObserved: true,
    weiQianliExplicitWuLiBoundaryObserved: false,
    missingWuLiBoundaryRemainsPrimaryDiscoveryDeficit: true,
    searchSnippetMayCountAsAuthorityEvidence: false,
    modelSynthesisMayCountAsAuthorityEvidence: false,
    generalKnowledgeMayCountAsAuthorityEvidence: false,
    numericCalibrationMayCountAsNormativeAuthority: false,
    qualitativeOrderingMayCountAsBinaryThreshold: false,
    crossCandidateCompositionPerformed: false,
    crossCandidateCompositionAuthorized: false,
    partialCandidatesMayBeCombinedToCloseGap: false,
    noCandidateFoundMayBeConvertedToDefaultRule: false,
    visibleStemBinaryEffectiveInteractionEligibilityResolved: false,
    effectiveInteractionSetResolved: false,
    thresholdRuleCreatedByThisGate: false,
    damageEvaluationAuthorized: false,
    i98KeDamageVocabularyEvaluationResolved: false,
    i98ResearchMethodologyMaterializationAuthorized: false,
    hiddenStemInteractionEligibilityGapRemains: true,
    hiddenStemAuthorityGap:
      'SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED',
    methodologyDefinitionCreatedByThisGate: false,
    ruleDefinitionCreatedByThisGate: false,
    registrySnapshotMutatedByThisGate: false,
    reviewAttestationCreatedByThisGate: false,
    noTrackedRelationTouchSemanticsRemainUnchanged: true,
    structuralRelationKindMutationAuthorized: false,
    sourceActivationVerdictAuthorized: false,
    sourcePersistenceVerdictAuthorized: false,
    sourceEffectiveSupportVerdictAuthorized: false,
    relativeForceVerdictAuthorized: false,
    clashWinnerVerdictAuthorized: false,
    rescueEffectAuthorized: false,
    clashSettlementAuthorized: false,
    crossRelationPrecedenceAuthorized: false,
    targetPostRelationRootState: 'not_determined',
    effectiveMechanismForceVerdict: 'not_determined',
    relationSpecificUsefulnessHarmfulness: 'not_determined',
    classificationAuthorized: false,
    numericScoringAuthorized: false,
    recommendedNextGate:
      'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_READINESS_REVIEW',
    notes: [],
  } as const;

  return { ...base, ...overrides } as unknown as ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdAuthorityCandidateDiscoveryEvidenceReport;
}

describe('I121 source 克 visible-stem threshold 无力 boundary targeted authority discovery readiness', () => {
  test('freezes targeted 无力 discovery readiness without acquiring authority', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(report.status).toBe(
      'RESOLVED_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_READINESS',
    );
    expect(report.decision).toBe(
      'WU_LI_BOUNDARY_TARGETED_DISCOVERY_AUTHORIZED_SINGLE_CANDIDATE_FULL_SIX_CONTRACT_PRESERVED_NO_CROSS_SOURCE_EQUIVALENCE',
    );
    expect(report.targetedDiscoveryMayProceed).toBe(true);
    expect(report.targetRequirementId).toBe('WU_LI_BOUNDARY_SEMANTICS_AND_EXCEPTIONS');
    expect(report.candidateDiscoveryPerformedByThisGate).toBe(false);
    expect(report.authorityAcquiredByThisGate).toBe(false);
  });

  test('requires an explicit visible-stem 克 semantic boundary for 无力 or 無力', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(report.targetLiteralForms).toEqual(['无力', '無力']);
    expect(report.visibleStemKeContextRequired).toBe(true);
    expect(report.literalWuLiOrOrthographicEquivalentRequired).toBe(true);
    expect(report.explicitSemanticBoundaryStatementRequired).toBe(true);
    expect(report.explicitNoInteractionMeaningIfClaimedMustBeSourceLocal).toBe(true);
    expect(report.explicitNonZeroOrResidualForceMeaningIfClaimedMustBeSourceLocal).toBe(true);
    expect(report.explicitExceptionConditionsRequiredIfBoundaryIsConditional).toBe(true);
    expect(report.simplifiedTraditionalOrthographicEquivalenceAllowed).toBe(true);
  });

  test('preserves the I119 single-candidate full-six admission contract', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(report.discoveryMode).toBe(
      'TARGET_WU_LI_BOUNDARY_WHILE_PRESERVING_SINGLE_CANDIDATE_FULL_SIX_REQUIREMENT_ADMISSION',
    );
    expect(report.sameCandidateMustExposeAllSixI118SemanticLociBeforeRegistration).toBe(true);
    expect(report.targetedRequirementHitAloneMayAuthorizeRegistration).toBe(false);
    expect(report.targetedRequirementHitAloneMayAuthorizeThreshold).toBe(false);
    expect(report.candidateMayProceedToCoverageEvaluationOnlyAfterFullSixLocatorAdmission).toBe(true);
    expect(report.crossCandidateCompositionAuthorized).toBe(false);
    expect(report.partialCandidateCompositionAuthorized).toBe(false);
  });

  test('forbids synonym substitution and cross-source equivalence fabrication', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(report.semanticSynonymWithoutWuLiMaySatisfyTarget).toBe(false);
    expect(report.buNengXiangKeMayBeMappedToWuLiWithoutSameSourceStatement).toBe(false);
    expect(report.buYiKeLunMayBeMappedToWuLiWithoutSameSourceStatement).toBe(false);
    expect(report.buZuoKeLunMayBeMappedToWuLiWithoutSameSourceStatement).toBe(false);
    expect(report.lingEffectMayBeMappedToWuLiWithoutSameSourceStatement).toBe(false);
    expect(report.existingI107WuLiTextAutomaticallyDefinesBoundary).toBe(false);
    expect(report.weiQianliBuNengXiangKeAutomaticallyDefinesI107WuLi).toBe(false);
    expect(report.searchSnippetMayCountAsAuthorityEvidence).toBe(false);
    expect(report.modelSynthesisMayCountAsAuthorityEvidence).toBe(false);
    expect(report.generalKnowledgeMayCountAsAuthorityEvidence).toBe(false);
    expect(report.numericCalibrationMayCountAsNormativeAuthority).toBe(false);
  });

  test('allows only governed reinspection of the strongest rejected candidate', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(report.weiQianliCandidateMayBeReinspectedForWuLiInVerifiedSameWorkText).toBe(true);
    expect(report.alternateEditionOrTranscriptionTextMayBeBorrowedWithoutIdentityResolution).toBe(false);
    expect(report.strongestRejectedCandidateMayBePromotedByThisGate).toBe(false);
    expect(report.candidateRegisteredByThisGate).toBe(false);
    expect(report.requirementCoverageEvaluatedByThisGate).toBe(false);
  });

  test('keeps threshold, damage, hidden-stem, settlement, scoring, and classification authority closed', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(report.visibleStemBinaryEffectiveInteractionEligibilityResolved).toBe(false);
    expect(report.effectiveInteractionSetResolved).toBe(false);
    expect(report.thresholdRuleCreatedByThisGate).toBe(false);
    expect(report.damageEvaluationAuthorized).toBe(false);
    expect(report.i98KeDamageVocabularyEvaluationResolved).toBe(false);
    expect(report.i98ResearchMethodologyMaterializationAuthorized).toBe(false);
    expect(report.hiddenStemInteractionEligibilityGapRemains).toBe(true);
    expect(report.hiddenStemAuthorityGap).toBe(
      'SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED',
    );
    expect(report.clashSettlementAuthorized).toBe(false);
    expect(report.crossRelationPrecedenceAuthorized).toBe(false);
    expect(report.classificationAuthorized).toBe(false);
    expect(report.numericScoringAuthorized).toBe(false);
  });

  test('is deterministic and routes valid readiness to targeted 无力 discovery evidence', () => {
    const first = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );
    const second = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120(),
    );

    expect(first.reviewId).toBe(second.reviewId);
    expect(first.recommendedNextGate).toBe(
      'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_EVIDENCE',
    );
    expect(first.methodologyDefinitionCreatedByThisGate).toBe(false);
    expect(first.ruleDefinitionCreatedByThisGate).toBe(false);
    expect(first.registrySnapshotMutatedByThisGate).toBe(false);
    expect(first.reviewAttestationCreatedByThisGate).toBe(false);
  });

  test('fails closed when I120 no longer proves the exact missing-无力 discovery frontier', () => {
    const report = buildI121ChallengeCombinationSupportChannelUntouchedSupportEffectSourceKeVisibleStemInteractionThresholdWuLiBoundaryTargetedAuthorityDiscoveryReadinessReview(
      i120({ strongestRejectedCandidateMissingRequirementIds: [] }),
    );

    expect(report.status).toBe('I120_UNRESOLVED_OR_INVALID');
    expect(report.decision).toBe('WU_LI_BOUNDARY_TARGETED_DISCOVERY_READINESS_NOT_ESTABLISHED');
    expect(report.targetedDiscoveryMayProceed).toBe(false);
    expect(report.discoveryMode).toBe('NONE');
    expect(report.targetRequirementId).toBeNull();
    expect(report.targetLiteralForms).toEqual([]);
    expect(report.recommendedNextGate).toBe(
      'UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_AUTHORITY_CANDIDATE_DISCOVERY_EVIDENCE',
    );
  });
});
