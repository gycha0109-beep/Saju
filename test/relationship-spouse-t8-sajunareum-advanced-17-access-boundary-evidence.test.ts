import { describe, expect, test } from 'vitest';
import { deterministicContentHash } from '../src/interpretation/rule-registry.js';
import { buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence } from '../src/research/relationship-spouse-t8-innercipher-2026-product-contract-direct-body-boundary-evidence.js';
import {
  RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CANDIDATE,
  RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CONTROL_IDS,
  RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_EVIDENCE_VERSION,
  buildRelationshipSpouseT8SajunareumAdvanced17AccessBoundaryEvidence,
} from '../src/research/relationship-spouse-t8-sajunareum-advanced-17-access-boundary-evidence.js';

describe('Relationship spouse T8 Sajunareum Advanced Lesson 17 access-boundary evidence', () => {
  test('pins the current public lesson identity and exact frontier', () => {
    const candidate = RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CANDIDATE;
    expect(candidate.publisher).toBe('Sajunareum');
    expect(candidate.koreanPublisher).toBe('사주나름');
    expect(candidate.lesson).toBe('Advanced Lesson 17');
    expect(candidate.frontierIssue).toBe(552);
    expect(candidate.sourceClass).toBe('current_public_educational_editorial_methodology_lesson');
    expect(candidate.publicUrls.english).toMatch(/sajunareum\.com\/en\/study\/adv\/17/);
    expect(candidate.publicUrls.korean).toMatch(/sajunareum\.com\/study\/adv\/17/);
  });

  test('preserves the indexed role-neutral method signal without promoting it', () => {
    const signal = RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CANDIDATE.indexedDiscoveryEvidence;
    expect(signal.exactPageIdentityObserved).toBe(true);
    expect(signal.englishAndKoreanTargetSpecificSectionsObserved).toBe(true);
    expect(signal.historicalMaleWealthFemaleOfficerFormulaExplicitlyDescribed).toBe(true);
    expect(signal.historicalFormulaExplicitlyCritiquedAsEraBoundGrammar).toBe(true);
    expect(signal.modernMethodCentersDayBranchRegardlessOfGenderSignalObserved).toBe(true);
    expect(signal.wealthOfficerDemotedToConnectionCatalystSignalObserved).toBe(true);
    expect(signal.fourStepPartnerReadingProcedureObserved).toBe(true);
    expect(signal.stepOneDayBranchTenGodUsedForPartnerExpectationAndRole).toBe(true);
    expect(signal.stepTwoNatalDayBranchCombinationsAndClashesUsedForRelationshipBase).toBe(true);
    expect(signal.stepThreeTraditionalSpouseStarStateUsedAsReferenceLayer).toBe(true);
    expect(signal.stepFourTimingLayerSeparatedInProcedure).toBe(true);
    expect(signal.practiceReportReusesDayBranchTenGodAndNatalInteractions).toBe(true);
    expect(signal.indexedSignalIsStrongerThanPositionOnlySpousePalaceStatement).toBe(true);
    expect(signal.completeRoleNeutralNatalSelectorEstablishedFromIndexedExcerpts).toBe(false);
    expect(signal.completeBodyFreeOfRequiredSexConditionedDependencyEstablished).toBe(false);
    expect(signal.absenceOfSelectorBlockerFromCompleteBodyInferredFromIndexedExcerpts).toBe(false);
    expect(signal.indexedExcerptsTreatedAsCompleteDirectBody).toBe(false);
  });

  test('freezes the current public retrieval boundary', () => {
    const candidate = RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CANDIDATE;
    expect(candidate.currentAccessBoundary.publicSearchIndexRecoveredTargetSpecificContent).toBe(true);
    expect(candidate.currentAccessBoundary.directPageFetchAttempted).toBe(true);
    expect(candidate.currentAccessBoundary.directPageFetchDisposition).toBe('CACHE_MISS');
    expect(candidate.currentAccessBoundary.normalDirectDownloadAttempted).toBe(true);
    expect(candidate.currentAccessBoundary.normalDirectDownloadDisposition).toBe('DOWNLOAD_UNAVAILABLE');
    expect(candidate.currentAccessBoundary.completeDirectHtmlBodyAcquired).toBe(false);
    expect(candidate.currentAccessBoundary.fullPageDirectTraversalPerformed).toBe(false);
    expect(candidate.directBodySemanticReviewPerformed).toBe(false);
    expect(candidate.bodyLevelAdmissionDecisionMade).toBe(false);
    expect(candidate.semanticDisposition).toBe(
      'PUBLIC_INDEXED_HIGH_VALUE_ROLE_NEUTRAL_SIGNAL_NO_FULL_BODY_ADMISSION_DECISION',
    );
  });

  test('does not infer selector admission or blocker absence from indexed excerpts', () => {
    const candidate = RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CANDIDATE;
    expect(candidate.explicitRoleNeutralNatalMappingGapClosedByThisEvidence).toBe(false);
    expect(candidate.currentGovernedSemanticCorrespondenceGapClosedByThisEvidence).toBe(false);
    expect(candidate.relationshipT6InputGapClosedByThisEvidence).toBe(false);
    expect(candidate.selectorBoundary).toMatch(/complete-body confirmation/i);
    expect(candidate.selectorBoundary).toMatch(/neither positive admission nor negative rejection/i);
    expect(candidate.crossSourceSemanticStitching).toBe(false);
    expect(candidate.opaqueIdentifierGuessingPerformed).toBe(false);
    expect(candidate.accessControlCircumventionPerformed).toBe(false);
  });

  test('chains from InnerCipher #546 and preserves the two-of-five ledger', () => {
    const upstream = buildRelationshipSpouseT8InnerCipher2026ProductContractDirectBodyBoundaryEvidence();
    const report = buildRelationshipSpouseT8SajunareumAdvanced17AccessBoundaryEvidence();
    expect(report.upstreamEvidenceId).toBe(upstream.evidenceId);
    expect(report.exactUpstreamTwoOfFiveStateAccepted).toBe(true);
    expect(report.targetSpecificIndexedBodyEvidenceInspected).toBe(true);
    expect(report.indexedRoleNeutralNatalMethodSignalObserved).toBe(true);
    expect(report.completeDirectHtmlBodyInspected).toBe(false);
    expect(report.fullPageDirectTraversalPerformed).toBe(false);
    expect(report.directBodySemanticReviewPerformed).toBe(false);
    expect(report.bodyLevelAdmissionDecisionMade).toBe(false);
    expect(report.explicitRoleNeutralNatalMappingEstablished).toBe(false);
    expect(report.currentGovernedMethodSemanticCorrespondenceEstablished).toBe(false);
    expect(report.currentRelationshipT6InputPathEstablished).toBe(false);
    expect(report.qualifyingPrimaryWitnessRemainsClosed).toBe(true);
    expect(report.independentNormativeProvenanceRemainsClosed).toBe(true);
    expect(report.authorityGapsClosedCount).toBe(2);
    expect(report.authorityGapsOpenCount).toBe(3);
    expect(report.authorityAdmissionReady).toBe(false);
    expect(report.spouseT8ProducerReady).toBe(false);
    expect(report.productionPromotionReady).toBe(false);
    expect(report.productionState).toBe('HOLD');
  });

  test('keeps the complete control set active', () => {
    const report = buildRelationshipSpouseT8SajunareumAdvanced17AccessBoundaryEvidence();
    expect(report.crossSourceStitchingAuthorized).toBe(false);
    expect(report.controlIds).toEqual(RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CONTROL_IDS);
    expect(report.controlCount).toBe(
      RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_CONTROL_IDS.length,
    );
  });

  test('is deterministically content-addressed', () => {
    const first = buildRelationshipSpouseT8SajunareumAdvanced17AccessBoundaryEvidence();
    const second = buildRelationshipSpouseT8SajunareumAdvanced17AccessBoundaryEvidence();
    const { evidenceId, ...material } = first;
    expect(first.evidenceVersion).toBe(
      RELATIONSHIP_SPOUSE_T8_SAJUNAREUM_ADVANCED_17_ACCESS_BOUNDARY_EVIDENCE_VERSION,
    );
    expect(first.status).toBe('PUBLIC_INDEXED_HIGH_VALUE_ROLE_NEUTRAL_SIGNAL_NO_FULL_BODY_ADMISSION_DECISION');
    expect(second.evidenceId).toBe(first.evidenceId);
    expect(evidenceId).toBe(
      `relationship_spouse_t8_sajunareum_advanced_17_access_boundary_${deterministicContentHash(material).slice(0, 24)}`,
    );
    expect(first.recommendedNextAction).toBe(
      'RECOVER_DIRECT_COMPLETE_SAJUNAREUM_ADVANCED_17_BODY_OR_CONTINUE_SINGLE_SOURCE_PUBLIC_DIRECT_BODY_DISCOVERY',
    );
  });
});
