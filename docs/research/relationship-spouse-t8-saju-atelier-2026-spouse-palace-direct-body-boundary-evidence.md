# Relationship / Spouse T8 — Saju Atelier 2026 spouse-palace direct-body boundary

## Disposition

```text
DIRECT_BODY_FLEXIBLE_PARTNER_LANGUAGE_BUT_GENDERED_CALCULATION_BASE_PRESERVED_NEGATIVE_COMPLETE_ROLE_NEUTRAL_SELECTOR
```

This record freezes the directly traversed complete public HTML body for:

```text
publisher / site = Saju Atelier
title = Your Future Spouse Is Already in Your Chart: The Spouse Palace and the Marriage Star
public URL = https://sajuatelier.com/library/journal/spouse-palace-marriage-star-saju
source class = current public editorial / commercial methodology guide
frontier issue = #580
```

The page is useful because it is a current single-chart relationship-reading guide that explicitly discusses the Day-Branch spouse palace, the spouse-star calculation, modern flexible partner language, and the boundary between one-person natal reading and two-chart compatibility.

## Direct-body acquisition

The page is directly retrievable as complete HTML and was traversed as 120 lines:

```text
observed line range = 0-119
observed line count = 120
completeDirectHtmlBodyAcquired = true
fullPageDirectTraversalPerformed = true
directBodySemanticReviewPerformed = true
bodyLevelAdmissionDecisionMade = true
```

No PDF, preview, metadata substitute, login, payment, opaque identifier, or authenticated session is involved.

The page footer exposes a 2026 copyright year, but no publication date is asserted by this evidence.

## Exact body map

```text
identity / title                                  = lines 6-9
two-thread complete-reading structure             = lines 22-27
spouse-palace method                              = lines 31-37
spouse-star calculation and modern-language note  = lines 39-47
single-chart vs compatibility boundary            = lines 66-72
full-reading product description                  = lines 74-79
FAQ spouse-star calculation                       = lines 84-92
FAQ single-chart vs compatibility boundary        = lines 100-101
```

## 1. Complete reading requires both Palace and Star

The article separates marriage reading into two threads:

```text
Spouse Palace = a chart position / seat
Spouse Star   = a Ten-God role / partner symbol
```

It then states that a complete marriage reading checks both rather than collapsing them into one object.

Accordingly, the sex-common Day-Branch palace cannot by itself be treated as the page's complete spouse selector.

## 2. Day Branch spouse palace is a real sex-common positional layer

The source defines:

```text
Spouse Palace = natal Day Branch
```

and reads the branch's condition as the relationship seat.

This is a genuine single-native natal layer that does not depend on a second person's chart and is not itself conditioned on native sex.

However:

```text
spousePalaceIsSexCommonPositionalLayer = true
spousePalaceReplacesSexConditionedSpouseStarSelector = false
```

because the same article separately requires a spouse-star layer for a complete reading.

## 3. Spouse-star calculation remains explicitly sex-conditioned

The direct body defines the active spouse-star calculation as:

```text
male native   -> Wealth star
female native -> Officer star
```

The source openly acknowledges that this mapping comes from a heteronormative / patriarchal historical frame.

That acknowledgment does not remove the branch. The article immediately states that contemporary practitioners increasingly use flexible partner language across genders and orientations **while the calculation base stays fixed**.

This is the decisive distinction:

```text
flexible modern partner language = OBSERVED
gendered spouse-star calculation base = STILL OPERATIONAL
```

## 4. FAQ independently repeats the same calculation boundary

The FAQ again gives:

```text
man   -> Wealth
woman -> Officer
```

and again says contemporary practitioners may read the role more flexibly while the calculation base stays the same.

Thus the surviving sex branch is not an isolated historical note. It is restated in the page's final explanatory layer.

## 5. Single-native natal reading is clearly separated from compatibility

The article explicitly distinguishes:

```text
one person's spouse palace + spouse star reading
```

from:

```text
two complete charts compared through compatibility / 궁합
```

This is useful for T8 because the page is clearly capable of a single-native natal relationship reading.

But satisfying the single-native boundary does not satisfy role neutrality. The page's single-chart reading still uses the male-Wealth / female-Officer spouse-star calculation base.

## 6. Semantic decision

The frozen source-bounded disposition is:

```text
DIRECT_BODY_FLEXIBLE_PARTNER_LANGUAGE_BUT_GENDERED_CALCULATION_BASE_PRESERVED_NEGATIVE_COMPLETE_ROLE_NEUTRAL_SELECTOR
```

Positive findings:

```text
complete public body directly inspected                    = YES
single-native natal relationship method                    = YES
Day Branch spouse-palace positional layer                  = YES
modern flexible partner language                           = YES
single-chart vs compatibility distinction                  = YES
```

Blocking findings:

```text
complete reading still requires spouse star + palace       = YES
male native -> Wealth spouse-star calculation survives     = YES
female native -> Officer spouse-star calculation survives  = YES
FAQ says the calculation base stays the same               = YES
complete native-sex-independent spouse selector            = NO
complete partner-sex-independent spouse selector           = NO
```

Flexible language is therefore not promoted into selector authority.

## Safety and provenance boundary

No action in this review performs or authorizes:

```text
opaque identifier guessing
login/session bypass
institution-auth bypass
paywall bypass
DRM/decryption
TLS verification disable
```

The source is ordinary public HTML and requires none of those actions.

## No cross-source stitching

Saju Atelier is adjudicated only on its own complete body.

Its flexible partner language, Day-Branch spouse-palace method, and Wealth/Officer spouse-star calculation are not combined with Sajunareum, InnerCipher, OpenFate, Saju Works, Cheonmyeonggwan, Clarify, Atlas Destiny, or another source to manufacture a role-neutral selector the page itself does not publish.

## Authority ledger

Unchanged:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 2/5
authorityGapsOpen = 3/5
authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

No spouse producer, governed semantic correspondence, T6 input path, interpretation pack, consumer narrative, compatibility activation, or Production promotion is authorized by this evidence.

## Upstream chain

This evidence directly consumes the merged Sajunareum Advanced Lesson 17 access-boundary evidence and accepts it only when the exact two-of-five authority state is still preserved:

```text
2 CLOSED
3 OPEN
Production = HOLD
```

A failure in that upstream state fails this evidence closed.

## Next authority action

Continue prioritizing a **single source with a complete public body** that itself publishes one operational spouse selector satisfying all of:

```text
single-source
single-native
natal-facts-only
operational / executable
native-sex-independent
partner-sex-independent
```

A sex-common spouse palace, modern inclusive terminology, criticism of historical gender roles, or a two-chart compatibility policy is insufficient unless the same source itself defines the complete role-neutral natal selector.
