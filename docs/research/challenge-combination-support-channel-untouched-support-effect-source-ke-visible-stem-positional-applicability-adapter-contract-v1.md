# I114 — Source 克 Visible-Stem Positional Applicability Adapter Contract

## Status

STRICT CLOSED

## Exact verified implementation

- code/test/export HEAD: `e44e5e9c46d639d6eda719f4ce387b2d23089113`
- CI: `#889` — SUCCESS
- verification: 172 test files / 1022 tests
- I114 tests: 8/8
- lint/typecheck/test/build: PASS

## Decision

`VISIBLE_STEM_POSITIONAL_TOPOLOGY_ADAPTER_CONTRACT_FROZEN_NO_EFFECT_OR_INTERACTION_VERDICT`

## Frozen scope

I114 freezes a narrow research adapter contract for exact directional `克` bindings whose source and target are both visible heavenly stems.

The canonical repository pillar order is:

```text
year -> month -> day -> hour
```

The source vocabulary is mapped to deterministic repository position topology as follows:

```text
邻干 + 力大 -> slot distance 1 -> ADJACENT_PILLAR_STEMS
隔干 + 次之 -> slot distance 2 -> ONE_INTERVENING_PILLAR_STEM
远干 + 无力 -> slot distance 3 -> TWO_INTERVENING_PILLAR_STEMS
```

`repositorySlotDistance` is topology only. It is not an interaction force score or a magnitude.

## Preconditions

A future evidence adapter may materialize I114 positional evidence only when:

- the pair already exists as an exact directional `克` binding;
- both participants are visible heavenly stems;
- source and target component identities are preserved;
- source-to-target direction remains inherited from the directional binding;
- the pair is not a self-pair.

## Explicit exclusions

The following cannot participate in this adapter:

- hidden-stem participants;
- raw EarthlyBranch element identity;
- non-`克` pairs;
- reversed or transitive inferred pairs.

The hidden-stem authority gap remains:

`SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED`

## Source-vocabulary ceiling

`力大`, `次之`, and `无力` are preserved source qualitative vocabulary only.

They must not be converted into:

- numeric force weights;
- relative-force rankings;
- effective-interaction verdicts;
- damage outcomes;
- damage magnitudes;
- settlement outcomes.

In particular:

`远干无力 != no interaction`

## Non-equivalences preserved

```text
slot distance != force magnitude
positional topology != interaction eligibility
positional topology != damage verdict
positional topology != damage magnitude
qualitative source label != numeric weight
远干无力 != no interaction
visible-stem positional evidence != effective interaction set
visible-stem positional evidence != I98 克 damage-vocabulary resolution
```

## Downstream guards

```text
visibleStemEffectiveInteractionEligibilityAuthorized = false
effectiveInteractionSetResolved = false
damageOutcomeAuthorized = false
damageMagnitudeAuthorized = false
settlementOutcomeAuthorized = false
i98KeDamageVocabularyEvaluationResolved = false
i98ResearchMethodologyMaterializationAuthorized = false
sourceActivationVerdictAuthorized = false
sourcePersistenceVerdictAuthorized = false
sourceEffectiveSupportVerdictAuthorized = false
relativeForceVerdictAuthorized = false
clashWinnerVerdictAuthorized = false
rescueEffectAuthorized = false
clashSettlementAuthorized = false
crossRelationPrecedenceAuthorized = false
structuralRelationKindMutationAuthorized = false
targetPostRelationRootState = not_determined
effectiveMechanismForceVerdict = not_determined
relationSpecificUsefulnessHarmfulness = not_determined
classificationAuthorized = false
numericScoringAuthorized = false
```

## Next gate

`UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_POSITIONAL_APPLICABILITY_EVIDENCE`

I115 may materialize deterministic current-chart positional evidence for eligible visible-visible directional `克` bindings. It must not convert that evidence into interaction eligibility, damage, magnitude, settlement, relative-force, classifier, or numeric-scoring outcomes.
