# Relationship / Spouse T8 — OpenFate 2026 current-methodology access boundary

## Disposition

```text
PUBLIC_INDEXED_BODY_PARTIAL_NO_FULL_BODY_ADMISSION_DECISION
```

This record freezes the legitimate public-access boundary for a previously unprocessed 2026 OpenFate Wiki methodology source family that directly targets modern family-role mappings, spouse-star matching, spouse palace, and identity/non-inference rules.

It is **not** a body-level selector admission or rejection.

## Source family

```text
source = OpenFate Wiki
methodology domain = Bazi / Four Pillars
source class = current public editorial / methodology reference
observed review dates = 2026-07-17 through 2026-07-22
frontier issue = #532
```

Target pages:

```text
https://wiki.openfate.ai/ko/bazi/life-themes/applying-family-role-mappings-to-modern-families
https://wiki.openfate.ai/ko/bazi/relationships-compatibility/spouse-star-matching-in-bazi-compatibility
https://wiki.openfate.ai/ko/bazi/relationships-compatibility/spouse-palace-in-bazi
https://wiki.openfate.ai/ko/bazi/life-themes/what-are-the-six-family-roles-in-bazi
https://wiki.openfate.ai/ko/bazi/four-pillars/six-relative-stars-and-palaces
```

## Why this source family matters

The publicly indexed material is unusually close to the currently open Relationship / Spouse T8 semantic boundary.

Target-specific indexed sections state or signal that:

- traditional family-role tables are interpretive mappings, not detectors of actual kinship or identity;
- actual family roles should be defined by the user rather than inferred from a natal chart;
- same-sex partners and chosen-family roles are valid modern-family examples;
- sex and sexual orientation must not be inferred from chart symbols;
- spouse star and spouse palace are separate evidence layers;
- a spouse-star method should declare its role policy before interpretation;
- a role-neutral policy may be selected;
- Day Branch remains the traditional spouse-palace positional layer.

These are meaningful discovery signals for a current-methodology source. They are not promoted to an executable spouse selector.

## Exact access boundary

The public search index exposes exact page identity and substantial target-specific page sections. Direct fetch attempts against the decisive OpenFate pages through the available browser surface returned `Cache miss`.

Therefore:

```text
publicSearchIndexRecoveredTargetSpecificContent = true
directPageFetchAttempted = true
directPageFetchDisposition = CACHE_MISS
completeDirectHtmlBodyAcquired = false
fullPageDirectTraversalPerformed = false
completePdfAcquired = false
renderedPageCount = 0
directBodySemanticReviewPerformed = false
bodyLevelAdmissionDecisionMade = false
semanticDisposition = PUBLIC_INDEXED_BODY_PARTIAL_NO_FULL_BODY_ADMISSION_DECISION
```

The indexed excerpts are deliberately not relabeled as complete direct-body review.

## What the indexed material does establish at discovery level

### Modern family-role mapping

The indexed `현대 가족 구조에는 육친 대응을 어떻게 적용할까?` material distinguishes traditional symbolic mappings from actual family identity. It explicitly mentions adoption, remarriage, single-parent families, same-sex partners, and chosen family, and says the actual relevant person or role should be defined by the person seeking the interpretation.

It also exposes a strong non-inference boundary: chart symbols are not evidence of kinship, sex, sexual orientation, family legitimacy, caregiving quality, or another person's fate.

### Spouse-star methodology

The indexed `사주 궁합에서 배우자성을 비교하는 법` material says the analyst should declare the spouse-role policy before reading. It preserves the existence of a traditional sex-conditioned mapping while also signaling that a role-neutral policy may be selected.

That signal is not enough by itself to close `EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING`. A complete qualifying selector would need to define, inside the same source, how natal facts determine the spouse-role mapping without a surviving native-sex branch, partner-sex branch, or required external role assignment.

The surfaced page is also a compatibility context that evaluates roles across charts; that context is not silently converted into a single-native natal contract.

### Spouse palace

The indexed OpenFate material preserves Day Branch as the traditional spouse-palace location layer. As in prior evidence, this is a role-neutral **position** but not by itself a complete spouse-role selector.

## What is not inferred

The current record does **not** infer that the complete OpenFate source family lacks a qualifying selector. The decisive complete pages were not directly traversed.

Specifically:

```text
completeRoleNeutralNatalSelectorFormulaObservedInIndexedExcerpts = false
absenceOfSelectorFromCompleteBodyInferredFromIndexedExcerpts = false
```

This distinction is mandatory: absence from surfaced indexed excerpts is not evidence of absence from the complete source body.

## Safety and provenance boundary

No action in this review authorizes or performs:

```text
opaque identifier guessing
login/session bypass
institution-auth bypass
paywall bypass
DRM/decryption
TLS verification disable
```

No cached hidden route or opaque content identifier is guessed.

## No cross-source stitching

OpenFate's indexed modern-role signals are not combined with Kweon Sujeong 2021, Hong Yooseon 2022, Kim Sanghan 2026, Lee Ockhwa 2024, Yoon Sangheum 2023, Jeon Jeonghun, Kim Mantae, Nam/Kim 2018, or another source to manufacture or reject the missing selector.

In particular, this record does not combine one source's `Day Branch = spouse palace` layer with another source's modern role language to construct an authority that no single source itself publishes.

## Authority ledger

Unchanged:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 2/5
authorityGapsOpen = 3/5
authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

No spouse producer, rule, claim, pack, narrative, preview, compatibility activation, or production activation is authorized.

## Repository race correction

The discovery phase had observed main at:

```text
4e034af344ad5cd3ffcff500e42ce6cd3096214f
```

Before implementation, unrelated general-natal PR #531 advanced main to:

```text
2122dd8f51accbdc7bd9f80879dc81557b09183e
```

The stale SHA is not used as the implementation or merge base. This evidence is authored from the later fresh main.

## Next authority action

Priority order:

1. recover a directly traversable complete OpenFate target body through a normal public route and perform a source-bounded review; or
2. continue searching for a single public-complete source that itself publishes a spouse selector satisfying:

```text
single-source
single-native
natal-facts-only
operational / executable
native-sex-independent
partner-sex-independent
```

until one source closes the missing mapping without cross-source stitching.
