# General Natal Conclusion T8 — Production Admission Readiness v2

Issue: #730  
Audit base: `3b724b087311a9831cc695466b1fff9861ad57aa`

## Purpose

This refresh evaluates the existing General Natal conclusion candidate after the passage-witness subset from #789 entered `main`.

It does not promote lifecycle state, rule provenance quality, reviewer status, trust authority, or Production.

## Runtime path

The bounded research path remains reachable:

```text
CanonicalSajuSnapshot.derivedFacts.tenGods
  → T5 TEN_GOD_FAMILY_*_PRESENT
  → T8 general conclusion claims
```

For this candidate only:

```text
GyeokgukResolverRequiredForThisCandidate = false
```

This does not remove Gyeokguk from the wider natal interpretation architecture.

## Passage evidence now consumed directly

The readiness artifact now calls the merged passage-witness evidence instead of ignoring it.

Current exact result:

```text
exactCandidateRuleCount = 15
passagePinnedRuleCount = 10
singleSourcePassagePinnedRuleCount = 1
multiSourcePassagePinnedRuleCount = 9
unsupportedExactRuleCount = 5
exactCurrentRuleProductionSupportedCount = 0
```

The ten passage-pinned rules establish bounded classical taxonomy or structural propositions. They do not establish the current full consumer wording as Production-supported output.

The PEER family boundary remains intentionally narrower:

```text
peerFamilyPassageWitnessEstablished = true
peerFamilyMultiSourceWitnessEstablished = false
```

The registered `三命通會` volume-five source does not directly establish the normalized `比肩 + 劫財` peer grouping, so it is not counted as a second peer witness.

## Source and provenance state

The candidate still has two registered sources and both use:

```text
provenanceTier = cross_reference
```

That tier is permitted by the current Production execution contract.

However all exact candidate rules still declare:

```text
provenanceQuality = secondary_only
reviewerStatus = unreviewed
```

while Production accepts only:

```text
primary_supported
multi_source_supported
```

Therefore:

```text
sourceReferenceRegistered = true
sourceTierPermitted = true
productionRuleProvenanceReady = false
productionEligibleProvenanceByRegistryContract = false
```

The passage evidence independently also reports:

```text
sourceIntegrityQualificationStillRequired = true
modernConsumerSemanticBridgeStillRequired = true
exactCurrentRuleProductionSupportedCount = 0
exactConsumerPassageSupportReady = false
```

A passage witness is evidence identity, not automatic rule-quality promotion.

## Lifecycle and review authority

Current candidate lifecycle remains:

```text
pack.status = research
methodology.status = research
rule.status = research
```

Current review authority remains absent:

```text
bundleReviewAttestationCount = 0
bundleReviewerTrustContextPresent = false
bundleTrustedReviewerGrantCount = 0
trustPinnedDomainAttestationAuthorityEstablished = false
domainReviewedLifecycleAuthorityEstablished = false
```

No reviewer, trust grant, or attestation is synthesized by this audit.

## Production verdict

```text
researchRuntimeReachable = true
GyeokgukResolverRequiredForThisCandidate = false

passageWitnessSubsetEstablished = true
passagePinnedRuleCount = 10
exactCurrentRuleProductionSupportedCount = 0

sourceReferenceRegistered = true
sourceTierPermitted = true
productionRuleTestCoverageReady = true

productionPackLifecycleReady = false
productionMethodologyLifecycleReady = false
productionRuleLifecycleReady = false
productionRuleProvenanceReady = false
productionRuleReviewerReady = false
exactConsumerPassageSupportReady = false
trustPinnedDomainAttestationAuthorityEstablished = false

productionEligibleProvenanceEstablished = false
domainReviewedLifecycleAuthorityEstablished = false
productionAdmissionAuthority = false
Production = HOLD
```

## Next evidence frontier

The next work is not another generic readiness audit. It is to close the provenance gap itself:

1. qualify the pinned source witnesses against reliable editions or scans;
2. establish exact reviewed consumer-semantic bridges for the supported structural relations, or narrow the consumer outputs to what the passages actually support;
3. keep the five unsupported exact outputs research-only unless separate evidence is obtained;
4. only after exact support exists, reconsider rule provenance quality;
5. after provenance authority is real, obtain domain reviewer/trust/attestation authority and perform lifecycle promotion separately.

## Guardrails

No lifecycle promotion, Production pack creation, registry activation, reviewer/trust fabrication, ProductHost change, Narrative/LLM semantic expansion, Gyeokguk/強弱/旺衰 implementation, SKU, Payment, Entitlement, Refund, or Commerce activation is performed here.
