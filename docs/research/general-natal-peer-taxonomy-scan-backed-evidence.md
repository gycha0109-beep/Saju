# General Natal — Samyeong Volume-7 Peer-Taxonomy Scan-Backed Evidence

Issue: #812

## Purpose

This artifact brings the peer-family second classical source added by #807 into the scan-backed provenance evidence surface.

It records only evidence that is currently established and deliberately keeps exact scan-page and Production provenance authority fail-closed.

## Bound source

```text
sourceId = SRC-SAMYEONG-TONGHOE-V7-FOUR-LIBRARIES-PEER-TAXONOMY
text = 三命通會（四庫全書本）卷七
section = 兄弟
bounded proposition = 兄弟者，即劫財比肩
```

The pinned Wikisource revision remains the repository transcription identity used by the candidate.

The evidence builder also binds the source to the exact content-addressed `0.2.0-research` peer-family T5 rule in the registry snapshot.

## Scan-backed authority located

A same-edition volume-seven scan is publicly exposed as:

```text
Wikimedia Commons
CADAL06066043 三命通會·卷七.djvu
欽定四庫全書本
Zhejiang University Library
CADAL06066043
198 pages
```

Chinese Text Project library metadata describes the twelve-volume 四庫全書 set as Zhejiang University Library material scanned by CADAL.

The CText volume-seven OCR surface states that it was generated by character recognition over the corresponding scan-backed original and contains the bounded peer-taxonomy proposition.

Therefore the bounded evidence established here is:

```text
sameEditionScanAuthorityLocated = true
sameEditionDigitizationFamilyEstablished = true
sameEditionScanOcrPropositionCorroborated = true
```

## What is not established

The available web tooling has not independently identified and visually compared the exact physical page or folio containing the sentence in the CADAL scan.

Accordingly:

```text
exactPhysicalPageOrFolioVerified = false
directScanImageComparisonCompleted = false
exactWitnessHashReproducedFromScan = false
exactTranscriptionIdentityEstablished = false
fullSourceIntegrityQualificationEstablished = false
```

The OCR surface is corroborating evidence, not a substitute for direct scan-image verification.

## Peer-rule scope remains unchanged

This evidence does not change rule semantics. The peer T5 rule continues to use:

```text
淵海子平 peer taxonomy
+
三命通會卷七 peer taxonomy
```

The Samyeong volume-five taxonomy source remains excluded from peer-family taxonomy support.

The downstream `PEER -> WEALTH` structural relation still requires exact `겁재` and is not widened to 比肩-only presence.

## Lifecycle and provenance boundary

Nothing is promoted by this artifact:

```text
peer rule status = research
peer rule provenanceQuality = secondary_only
peer rule reviewerStatus = unreviewed
sourceIntegrityQualificationEstablished = false
provenanceQualityPromotionAuthorized = false
domainReviewAuthorityEstablished = false
productionAdmissionAuthority = false
Production = HOLD
```

A future exact page/folio and direct scan-image comparison may strengthen source integrity, but those facts must be independently verified before the corresponding flags can change.

## External evidence surfaces

```text
Commons scan:
https://commons.wikimedia.org/wiki/File:CADAL06066043_%E4%B8%89%E5%91%BD%E9%80%9A%E6%9C%83%C2%B7%E5%8D%B7%E4%B8%83.djvu

CText library metadata:
https://ctext.org/library.pl?if=gb&res=6109

CText volume-7 scan-backed OCR:
https://ctext.org/wiki.pl?chapter=548506&if=gb
```

## Non-scope

No physical page is guessed. No OCR-to-image identity is claimed. No provenance-quality mutation, reviewer identity, review attestation, trust grant, lifecycle promotion, consumer semantic expansion, Gyeokguk, 強弱, 旺衰, Yongshin, timing, SKU, ProductHost, Narrative, LLM semantic expansion, or Commerce change is introduced.
