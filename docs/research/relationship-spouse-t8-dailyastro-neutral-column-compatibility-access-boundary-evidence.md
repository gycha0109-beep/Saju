# Relationship / Spouse T8 — DailyAstro neutral-column compatibility access boundary

## Disposition

```text
PUBLIC_INDEXED_OPERATIONAL_ROLE_NEUTRAL_TWO_CHART_COMPATIBILITY_SIGNAL_NO_FULL_BODY_NO_SINGLE_NATIVE_ADMISSION_DECISION
```

This record freezes the currently observable DailyAstro methodology signal **after** the repository authority transition introduced by Whisper #587.

```text
publisher / site = DailyAstro
title = Methodology
public URL = https://dailyastro.ai/en/methodology/
source class = current public app methodology / compatibility scoring rules
frontier issue = #586
```

DailyAstro exposes a materially useful operational role-neutral compatibility fallback. It does not close a new T8 authority gap because Whisper has already closed the explicit role-neutral natal-mapping evidence gap and the two remaining gaps are repository semantic correspondence and Relationship T6 input authority.

## 1. Upstream authority is now Whisper 3/5

The authoritative upstream evidence is:

```text
Whisper #585 / PR #587
DIRECT_BODY_DAY_MASTER_POLARITY_SPOUSE_SELECTOR_POSITIVE_EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING
```

Whisper directly traversed a complete 99-line public body and admitted a single-native natal spouse selector keyed to Day-Master polarity without native-sex, partner-sex, orientation, or second-chart input.

Accordingly the inherited ledger is:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = CLOSED
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 3/5
authorityGapsOpen = 2/5

authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

DailyAstro preserves this exact state and closes zero additional gaps.

## 2. Indexed operational neutral-column routing

Public search indexing exposes target-specific DailyAstro methodology content and states that the documented rules are the rules used by the service.

The indexed relationship subsystem contains seven lenses, including spouse and dating, and exposes separate Ten-God columns for:

```text
male spouse
female spouse
neutral
```

The indexed routing policy is:

```text
both genders known and different
  -> male side uses Wealth logic
  -> female side uses Officer logic

if either gender is missing
  -> neutral column on both sides

if both supplied genders are the same
  -> neutral column on both sides

other non-romantic lenses
  -> neutral column
```

This is operational routing evidence rather than merely inclusive wording.

## 3. Same-gender fallback and Nayin compatibility

The indexed methodology explains that a simple bidirectional extension of the classical male-Wealth / female-Officer spouse-star rule cannot be satisfied consistently for same-gender pairs.

It therefore exposes a gender-free fallback identified as Nayin marriage compatibility.

The indexed ordering / scoring signal is:

```text
generation   +6
same element +4
control      -2

ordering: generation > same element > control
```

These values are frozen only as DailyAstro indexed compatibility evidence.

They are **not** adopted as repository natal semantics, a spouse selector, a Relationship T6 input contract, or Production logic.

## 4. Exact method scope is two-chart compatibility

The decisive boundary is that the observed subsystem is pair-based.

Indexed scope signals include:

```text
both people's chart inputs are consumed
pair evaluation uses both people's Year Branch, Day Stem, and Day Branch
spouse and dating are mutual relationship lenses
bilateral relations are combined into one compatibility result
```

Therefore:

```text
methodScope = two_chart_compatibility
```

The DailyAstro method cannot be converted into a one-native natal selector by simply deleting the second chart.

## 5. Current direct-body boundary

The public page was attempted through the normal direct retrieval surface and currently returns:

```text
Cache miss
```

Accordingly:

```text
publicSearchIndexRecoveredTargetSpecificContent = true
directPageFetchAttempted = true
directPageFetchDisposition = CACHE_MISS
completeDirectHtmlBodyAcquired = false
fullPageDirectTraversalPerformed = false
directBodySemanticReviewPerformed = false
bodyLevelAdmissionDecisionMade = false
```

No indexed excerpt is relabeled as complete direct-body review.

No complete-body positive or negative DailyAstro admission verdict is made.

## 6. DailyAstro closes zero additional T8 authority gaps

The explicit role-neutral natal mapping is already CLOSED by Whisper.

DailyAstro does not establish either remaining gap:

```text
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN
```

An external two-chart compatibility policy cannot by itself define the repository-owned semantic correspondence for the admitted Whisper selector.

Likewise, it does not create a governed Relationship T6 input path.

Therefore:

```text
dailyAstroAdditionalAuthorityGapsClosedCount = 0
authorityGapsClosed = 3/5
authorityGapsOpen = 2/5
```

## 7. No semantic projection or cross-source stitching

Do not perform or authorize:

```text
remove DailyAstro's second chart to manufacture a natal selector
project DailyAstro bilateral weights into repository-owned natal semantics
replace or modify Whisper's admitted selector with DailyAstro compatibility logic
combine Whisper + DailyAstro to claim semantic correspondence
combine DailyAstro with Saju Atelier, Sajunareum, OpenFate, InnerCipher, Saju Works, Cheonmyeonggwan, Clarify, Atlas Destiny, or another source to create missing authority
```

The DailyAstro evidence remains source-bounded and compatibility-only.

## 8. Safety / access boundary

No action in this review performs or authorizes:

```text
opaque identifier guessing
login/session bypass
institution-auth bypass
paywall bypass
DRM/decryption
TLS verification disable
access-control circumvention
```

The source is assessed through ordinary public search indexing plus a normal direct page-open attempt.

## Authority ledger

Final state after this artifact remains:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = CLOSED
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 3/5
authorityGapsOpen = 2/5

authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

No spouse producer, claim type, interpretation pack, consumer narrative, preview default, compatibility activation, T6 path, or Production promotion is authorized.

## Next authority action

The main T8 priority is no longer more candidate hunting for the already-closed mapping gap.

Proceed to:

```text
GOVERN_REPOSITORY_OWNED_SEMANTIC_CORRESPONDENCE_FOR_ADMITTED_ROLE_NEUTRAL_SELECTOR
```

That governance task must independently define how the admitted Whisper selector corresponds to repository-owned semantics, preserve its school-dependent caveat, and keep Relationship T6 / Production closed until their own contracts are satisfied.
