# Relationship / Spouse T8 — DailyAstro neutral-column compatibility access boundary

## Disposition

```text
PUBLIC_INDEXED_OPERATIONAL_ROLE_NEUTRAL_TWO_CHART_COMPATIBILITY_SIGNAL_NO_FULL_BODY_NO_SINGLE_NATIVE_ADMISSION_DECISION
```

This record freezes the currently observable public-index evidence for:

```text
publisher / site = DailyAstro
title = Methodology
public URL = https://dailyastro.ai/en/methodology/
source class = current public app methodology / compatibility scoring rules
frontier issue = #586
```

The candidate is unusually relevant because the indexed methodology does not merely use neutral partner wording. It exposes an explicit operational routing rule with male, female, and neutral Ten-God columns and a neutral fallback used for same-gender or missing-gender cases.

However, the currently exposed method is pair-based compatibility logic, not a single-native natal spouse selector, and the complete page body could not be directly opened through the available public retrieval surface.

## Current public access boundary

Public search indexing exposes substantial target-specific methodology content, but a normal direct page-open attempt currently returns:

```text
Cache miss
```

Therefore:

```text
publicSearchIndexRecoveredTargetSpecificContent = true
directPageFetchAttempted = true
directPageFetchDisposition = CACHE_MISS
completeDirectHtmlBodyAcquired = false
fullPageDirectTraversalPerformed = false
directBodySemanticReviewPerformed = false
bodyLevelAdmissionDecisionMade = false
```

Indexed excerpts are discovery evidence only. They are not relabeled as complete direct-body review.

## 1. Operational neutral-column routing is publicly indexed

The indexed methodology describes seven compatibility lenses, including:

```text
spouse
dating
friend
colleague
business partner
boss
subordinate / helper
```

The same indexed material exposes separate Ten-God columns for:

```text
male spouse
female spouse
neutral
```

and publishes the routing policy:

```text
both genders known and different
  -> male side uses Wealth spouse logic
  -> female side uses Officer spouse logic

if either gender is missing
  -> neutral column on both sides

if both genders are known and the same
  -> neutral column on both sides

non-romantic lenses
  -> neutral column
```

This is an operational policy signal, not merely a vocabulary preference.

## 2. Same-sex fallback rationale is explicit in the index

The indexed methodology explains why it does not simply extend:

```text
male -> Wealth
female -> Officer
```

symmetrically to same-sex pairs.

For two men, requiring the classical wife-star relation in both directions would require mutually opposite control relations at once. The indexed page therefore treats that extension as mechanically unsatisfied and routes to a gender-free fallback instead.

That distinction matters: the source is not merely deleting gender labels while leaving the same sex-conditioned branch hidden underneath.

## 3. The indexed fallback is Nayin compatibility

The indexed material identifies the neutral fallback as a Nayin marriage-compatibility layer and exposes the ordering:

```text
generation > same element > control
```

with indexed example weights:

```text
generation   +6
same element +4
control      -2
```

This record preserves those values only as indexed DailyAstro compatibility evidence.

It does **not** promote them to a MyeongHa spouse selector or natal-rule authority.

## 4. The observed method is explicitly two-person compatibility

The indexed methodology repeatedly frames this subsystem as compatibility between two people.

Observed scope signals include:

```text
the same two birthdays can be evaluated through different relationship lenses
spouse and dating are mutual pair lenses
both people's year branch, day stem and day branch are used
a mutual lens reads both directions and produces one result
```

Accordingly:

```text
methodScope = two_chart_compatibility
```

This is the decisive T8 boundary.

## 5. Why this does not close the role-neutral natal-mapping gap

Relationship / Spouse T8 requires one source to itself publish an operational selector satisfying all of:

```text
single-source
single-native
natal-facts-only
operational / executable
native-sex-independent
partner-sex-independent
```

The indexed DailyAstro neutral column is operational and role-neutral in the fallback branch, but it consumes the second person's chart and evaluates bilateral compatibility relations.

Therefore it does not currently establish:

```text
singleNativeRoleNeutralNatalSelectorEstablished = true
```

Removing the second chart or projecting bilateral compatibility scores into a one-person natal selector would create a method the observed source has not published.

## 6. No complete-body positive or negative admission decision

The direct methodology page is currently unavailable through the normal public page-open surface.

For that reason this evidence does not claim either:

```text
complete-body positive T8 admission
```

or:

```text
complete-body negative T8 rejection
```

The body could contain additional scope, caveats, or a distinct single-native method not visible in the indexed excerpts. Conversely, the indexed excerpts alone cannot establish that such a method exists.

The correct state is therefore:

```text
bodyLevelAdmissionDecisionMade = false
```

## 7. No cross-source stitching

DailyAstro is adjudicated only as DailyAstro discovery evidence.

Do not combine its neutral compatibility column with:

```text
Saju Atelier spouse-palace layer
Sajunareum indexed Day-Branch method
OpenFate role-policy text
InnerCipher product-contract rules
Saju Works spouse-star rules
Cheonmyeonggwan modern reinterpretation
Clarify / Atlas Destiny / another source
```

in order to manufacture a single-native spouse selector.

Likewise, do not remove the second chart from DailyAstro's indexed compatibility procedure and claim the remainder is a natal selector.

## Safety / access boundary

No action in this review performs or authorizes:

```text
opaque identifier guessing
login/session bypass
institution-auth bypass
paywall bypass
DRM/decryption
TLS verification disable
access-control circumvention
```

The source was assessed only through ordinary public indexing and a normal direct page-open attempt.

## Authority ledger

Unchanged:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 2/5
authorityGapsOpen = 3/5

authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

No spouse producer, rule, claim type, interpretation pack, consumer narrative, preview default, compatibility activation, or production promotion is authorized by this evidence.

## Next authority action

Two paths remain valid:

1. recover and directly traverse the complete DailyAstro methodology body, then make a source-bounded decision about whether the same source also defines a qualifying single-native natal selector; or
2. continue searching for another single source with a directly accessible complete public body that itself publishes a selector satisfying:

```text
single-native
natal-facts-only
operational / executable
native-sex-independent
partner-sex-independent
```

The indexed DailyAstro neutral compatibility column is valuable discovery evidence, but it does not close the T8 authority gap.
