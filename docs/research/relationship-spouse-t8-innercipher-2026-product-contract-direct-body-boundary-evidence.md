# Relationship / Spouse T8 — InnerCipher 2026 product-contract direct-body boundary

## Scope

This record freezes the direct-body semantic boundary for the current public InnerCipher methodology article:

```text
publisher / author = InnerCipher
title = What Is the Spouse Palace in BaZi?
published = 2026-07-16
URL = https://www.innerciphers.com/en/articles/spouse-palace-bazi
frontier issue = #545
source class = current public commercial product methodology / editorial contract
```

This is research evidence only. It does not create or promote a production spouse selector.

## Complete public body

The complete public HTML body was directly retrieved and traversed:

```text
content type = text/html
observed line range = 0-145
observed line count = 146
completeDirectHtmlBodyAcquired = true
fullPageDirectTraversalPerformed = true
directBodySemanticReviewPerformed = true
bodyLevelAdmissionDecisionMade = true
```

No PDF, preview substitution, login, session, institution authentication, paywall, opaque identifier, DRM, decryption, or TLS-verification bypass was involved.

## Decisive source semantics

### 1. Day Branch is the spouse-palace positional layer

The article distinguishes the Day Stem as Day Master and the Day Branch as Spouse Palace. It repeatedly treats the Day Branch as one relationship lens rather than the whole relationship reading.

### 2. A fixed sex-common calculation sequence is published

The article gives a concrete single-native sequence:

```text
Day Master from Day Stem
→ Day Branch hidden stems
→ each hidden stem's Ten Gods relation to Day Master
→ position / season / visibility / roots / whole-chart context
```

A worked natal example applies that same sequence.

### 3. Current product contract rejects standalone gender-specific spouse-role rules

The article acknowledges that traditional schools may attach gender-specific spouse terminology to Ten Gods, but explicitly frames those rules as school-dependent and says they cannot stand alone as InnerCipher's public product contract.

This is a meaningful current-method signal: the public product contract does not treat `male -> Wealth / female -> Officer` literal spouse-role terminology as a standalone universal authority rule.

### 4. The replacement is not a complete role-neutral spouse-star selector

The same body also limits what its spouse-palace method means:

- hidden Ten Gods are structural relationships, not literal partner traits;
- the Spouse Palace is only one relationship lens;
- whole-chart context remains required;
- timing is separated from natal structure;
- a single natal chart is not two-person compatibility;
- the Full Report may use Day Branch hidden Ten Gods as one input alongside the rest of the chart.

No complete source-authored mapping is published that deterministically replaces a spouse-star selector while being simultaneously:

```text
single-native
natal-facts-only
native-sex-independent
partner-context-independent
complete / operational
```

The source therefore moves away from binary literal spouse-star personification, but does not fill the missing complete selector authority.

## Semantic disposition

```text
DIRECT_BODY_CURRENT_PRODUCT_CONTRACT_REJECTS_STANDALONE_GENDERED_SPOUSE_RULES_POSITIVE_NEUTRAL_SPOUSE_PALACE_METHOD_NEGATIVE_COMPLETE_ROLE_NEUTRAL_SPOUSE_SELECTOR
```

This means:

- complete current public body was directly reviewed;
- current external product-contract semantics are explicit;
- standalone gendered spouse-role rules are rejected as universal product authority;
- a sex-common spouse-palace / hidden-stem / Ten-God contextual method is positively observed;
- but no complete role-neutral spouse-star selector is published.

## Governed semantic correspondence boundary

InnerCipher is an external current product-methodology example. It does not by itself establish MyeongHa/Saju's governed semantic correspondence. External product behavior cannot silently become repository authority.

Therefore this evidence closes none of the three remaining gaps:

```text
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN
```

## No cross-source stitching

No InnerCipher statement is combined with `bazi-life-curves`, Saju Works, Cheonmyeonggwan, OpenFate, Kweon Sujeong, Hong Yooseon, or another source to manufacture a selector not published by InnerCipher itself.

## Authority ledger

Unchanged:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 2/5
authorityGapsOpen = 3/5
authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

## Next frontier

Continue single-source discovery for a public complete body or executable method that itself publishes a complete role-neutral spouse selector. Do not infer the missing selector from a neutral spouse-palace method, inclusive language, or multiple partial sources.
