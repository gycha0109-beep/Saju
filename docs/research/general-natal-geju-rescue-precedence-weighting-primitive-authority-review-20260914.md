# General Natal Gyeokguk — rescue precedence and weighting primitive authority review (2026-09-14)

## Decision

```text
RESCUE_PRECEDENCE_AND_WEIGHTING_PRIMITIVE = PARTIALLY_AUTHORIZED
DIRECT_SOURCE_RESCUE_MATERIALITY = OBSERVED
DIRECT_SOURCE_RESCUE_EXAMPLE_CATALOG = OBSERVED
DIRECT_SOURCE_RELATIVE_WEIGHTING_PRINCIPLE = OBSERVED
DIRECT_SOURCE_FIXED_PRECEDENCE_ORDER = NOT_OBSERVED
SOURCE_RESCUE_CATALOG_EXHAUSTIVE = false
CANONICAL_RESCUE_CLAUSE_INPUT_RESOLUTION = BLOCKED
GENERALIZED_RESCUE_EFFECT_PREDICATE = UNAUTHORIZED
GENERALIZED_RESCUE_PRECEDENCE_WEIGHTING_PREDICATE = UNAUTHORIZED
NUMERIC_WEIGHTING = UNAUTHORIZED
PRODUCTION_FACT_EMISSION = false
```

This review addresses only the remaining `rescue_precedence_and_weighting` primitive from the General Natal establishment frontier. It does not create a chart-level rescue matcher or terminal settlement rule.

## 1. Upstream authority

This review chains to the already merged boundaries:

```text
#517 establishment outcome representation
#519 mixed-outcome application review
#539 刑沖破害 effect primitive authority review
```

#517 already preserves `救應` as an intervention layer while recording rescue precedence as unauthorized. #519 keeps intervention resolution, overlap resolution, and terminal state unauthorized. #539 confirms that even the source-direct `刑沖` materiality cannot be converted into automatic establishment effect from current canonical structural matches.

## 2. Direct source

Selected source family:

```text
子平真詮 / 子平真詮評注
論用神成敗救應
```

The source directly states:

```text
敗中有成，全憑救應
```

and then asks `何謂救應？` before listing concrete intervention relations. The review preserves these as a research-only catalog, including:

```text
官逢傷 -> 透印以解之
雜煞 -> 合煞以清之
刑沖 -> 會合以解之
財逢劫 -> 透食以化之 / 生官以制之
財逢煞 -> 食神制煞以生財 / 存財而合煞
印逢財 -> 劫財以解之 / 合財而存印
食逢梟 -> 就煞以成格 / 生財以護食
煞逢食制，印來護煞 -> 逢財以去印存食
傷官生財透煞 -> 煞逢合
陽刃用官煞帶傷食 -> 重印以護之
建祿月劫用官，遇傷 -> 傷被合
建祿月劫用財帶煞 -> 煞被合
```

These are source-semantic example relations only. They are not canonical matchers.

## 3. Relative weighting is material but not algorithmic

The same section closes with:

```text
八字妙用，全在成敗救應，其中權輕權重，甚是活潑
```

This is enough to admit:

```text
relative weighting matters = OBSERVED
```

It is not enough to admit:

```text
fixed precedence order
numeric weight
numeric score
threshold
first-match-wins
source-list order as rank
deterministic overlap winner
```

The source explicitly presents the weighting as context-sensitive (`甚是活潑`) and addresses changing configurations rather than a fixed ordered table.

## 4. Canonical execution remains blocked

The current repository still lacks governed resolution for:

```text
canonical_candidate_identity
canonical_rescue_clause_input_resolution
source_example_to_canonical_matcher
multiple_rescue_overlap_resolution
rescue_effect_settlement_predicate
relative_weight_comparison_predicate
fixed_precedence_order
terminal_state_precedence
```

In particular, the direct example:

```text
刑沖而會合以解之
```

cannot be promoted to an executable rescue rule. #539 established that canonical structural relations cover only a structural `branch_clash` match among the broader `刑沖破害` vocabulary, and that structural presence does not establish Gyeokguk contextual effect or settlement.

## 5. Forbidden derivations

```text
listed rescue example -> universal matcher
source list order -> precedence order
權輕權重 -> numeric weight / score
first matching rescue -> winner
multiple rescue clauses -> deterministic ranking
absence of listed rescue -> no rescue
structural relation match -> rescue effect established
generic strength/clash router -> Gyeokguk establishment settlement
```

## 6. Production boundary

```text
GEJU_CANDIDATE = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE = NOT_EMITTED
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
NEXT_PRODUCTION_SKU = NONE
Commerce = HOLD
```

No ProductHost, Character, Narrative, LLM, API/browser, SKU, Payment, Entitlement, Refund, or Commerce behavior is changed.

## 7. Frontier after this review

This review closes the question of whether the selected source supplies a fixed rescue precedence/weighting algorithm: it does not. It preserves the direct rescue catalog and relative-weighting materiality while keeping execution fail-closed.

A later frontier must independently resolve governed canonical clause inputs and overlap/effect settlement before any `GEJU_ESTABLISHMENT_STATE` producer can be authorized.
