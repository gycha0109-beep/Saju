# I113 — Source 克 Interaction Eligibility Methodology Review

## Status

STRICT CLOSED

## Exact verified implementation

- code/test/export HEAD: `914a8d7f0f3f2297f5aee779103d0c86c8140672`
- CI: `#885` — SUCCESS
- verification: 171 test files / 1014 tests
- I113 tests: 8/8
- lint/typecheck/test/build: PASS

## Decision

`VISIBLE_STEM_POSITIONAL_METHODOLOGY_AVAILABLE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_GAP_REMAINS`

## Resolved boundary

I113 separates the already-resolved directional `克` vocabulary layer from effective-interaction eligibility.

The registered source evidence contains a verified visible-heavenly-stem positional vocabulary around `两干相克`, including the qualitative terms `邻干`, `隔干`, and `远干`. This is sufficient to authorize a narrow research adapter-contract review for visible-stem positional applicability.

It is not sufficient to resolve an effective interaction set.

## Visible-stem semantics

- source locator: verified
- qualitative positional applicability language: available
- source vocabulary preserved as `邻干 / 隔干 / 远干`
- repository pillar-distance mapping: unresolved
- qualitative positional language -> numeric distance: not authorized
- qualitative positional language -> numeric weight: not authorized
- `远干无力` -> no interaction: not authorized
- global relative-force inference: not authorized
- current-chart positional materialization: not authorized before a dedicated adapter contract

## Hidden-stem boundary

The same source provides branch-hidden-stem / five-element applicability language, but does not establish the required positional interaction-eligibility semantics for hidden-stem participants.

Therefore all of the following remain unresolved:

- visible stem -> hidden stem interaction eligibility
- hidden stem -> visible stem interaction eligibility
- hidden stem -> hidden stem interaction eligibility
- branch-hidden-stem positional interaction eligibility

Current authority gap:

`SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED`

Hidden-stem membership alone must not be promoted to an activated `克` interaction.

## Non-equivalences preserved

```text
directional 克 binding != effective interaction
same-chart co-presence != interaction eligibility
hidden-stem membership != activated 克 interaction
pillar distance alone != positional applicability
qualitative positional force != numeric weight
远干无力 != no interaction
克 interaction != damage outcome
克 interaction != damage magnitude
克 interaction != settlement outcome
```

## Downstream guards

The following remain closed:

```text
sourceActivationVerdictAuthorized = false
sourcePersistenceVerdictAuthorized = false
sourceEffectiveSupportVerdictAuthorized = false
relativeForceVerdictAuthorized = false
clashWinnerVerdictAuthorized = false
rescueEffectAuthorized = false
clashSettlementAuthorized = false
crossRelationPrecedenceAuthorized = false
structuralRelationKindMutationAuthorized = false
targetPostRelationRootState = not_determined
effectiveMechanismForceVerdict = not_determined
relationSpecificUsefulnessHarmfulness = not_determined
i98ResearchMethodologyMaterializationAuthorized = false
classificationAuthorized = false
numericScoringAuthorized = false
```

## Next gate

`UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_POSITIONAL_APPLICABILITY_ADAPTER_CONTRACT`

I114 must remain limited to a research contract translating source-visible-stem positional vocabulary into deterministic repository-side positional evidence semantics. It must not implement damage, magnitude, settlement, hidden-stem interaction eligibility, cross-relation precedence, a strength classifier, or numeric scoring.
