# General Natal Gyeokguk — Mixed-Qi Transparency Source Boundary

Date: 2026-09-12  
Status: research-only source-scope refinement  
Production authority: BLOCKED

## Purpose

This frontier follows the canonical observation substrate merged in PR #441. It narrows the selected `子平真詮 / 子平真詮評注` mixed-qi passage into an executable source-scope predicate without converting raw exact visible-stem observations into a Gyeokguk selection verdict.

The newly governed question is only:

> Does the resolved month branch fall inside the selected `四墓 / 雜氣` passage's branch scope?

For this source passage the bounded scope is:

```text
辰 戌 丑 未
```

No other month branch is assigned a negative universal Gyeokguk rule; it is simply outside this selected source passage.

## Source boundary

The selected passage `論雜氣如何取用` states, in bounded context:

- the four tomb/storehouse branches are treated as mixed qi because multiple stored contents can present different uses;
- transparency and branch meeting are selection axes;
- a single transparency corresponds to single use;
- multiple transparencies may be used together;
- transparency and branch meeting may be used together;
- later `有情 / 無情` judgment is distinct from the initial selection surface.

Repository source references reused:

```text
SRC-GEJU-ZIPING-ZHENQUAN-MIXED-QI-SELECTION
SRC-GEJU-ZIPING-ZHENQUAN-MIXED-QI-PLURAL-SELECTION
```

This frontier does not broaden those passages to all twelve month branches.

## Authority gained

The following source-scope operation is executable and testable:

```text
monthBranch in { 辰, 戌, 丑, 未 }
    -> selected mixed-qi passage scope applies
otherwise
    -> outside selected passage scope
```

The report also records that the selected source explicitly observes:

```text
single transparency -> single use
plural transparency -> plural/co-use
transparency + branch meeting -> joint use
```

These are source-rule observations, not canonical chart-selection facts.

## Authority not gained

The source wording uses `干 / 干頭` but does not, within the governed passage, enumerate a canonical year/month/day/hour slot admissibility policy.

Therefore this frontier does **not** authorize:

- treating every exact match recorded by PR #441 as source-semantic `透干`;
- choosing which of year/month/day/hour stems count for a future transparency predicate;
- deciding whether exact equality is sufficient under every context;
- applying branch-meeting transformation or selection effects;
- resolving `清`, conflict, precedence, damage, rescue, or transformation semantics;
- converting single/plural source use into a canonical zero/one/multiple `GEJU_CANDIDATE` contract;
- emitting `GEJU_CANDIDATE`;
- emitting `GEJU_ESTABLISHMENT_STATE=true/false`;
- granting General Natal production, ProductHost, Character, LLM, API, browser, or Commerce authority.

## Interaction with PR #441

PR #441 can observe, for every month hidden stem:

```text
visibleExactStemPositions
exactVisibilityObserved
```

Those fields remain raw canonical observations.

This frontier intentionally does not join them to the source word `透` because the canonical slot-admissibility bridge is not yet governed.

The next bridge must answer which canonical pillar positions are valid manifestations of the source's `干 / 干頭` wording, or explicitly prove that the source corpus leaves that question unresolved.

## Predicate ledger

One narrower predicate is now governed:

```text
MIXED_QI_MONTH_SOURCE_SCOPE_PREDICATE = AUTHORIZED
```

The five coarse Gyeokguk predicate-authority gaps remain open:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

No coarse gap is closed by this source-scope refinement.

## Fail-closed rule

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
Commerce = HOLD
```

Product, narrative, LLM, API presentation, browser UX, SKU, price, PSP, payment, entitlement, and refund layers must not manufacture the missing positional or selection semantics.

## Next frontier

The smallest honest next evidence-bearing step is the **canonical transparency positional-admissibility bridge** for the selected mixed-qi source scope.

It should determine whether governed source evidence can authorize which canonical pillar slots count as `透干`. If that cannot be established without relying on conflicting secondary interpretations, `VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING` remains open and the system stays fail-closed.
