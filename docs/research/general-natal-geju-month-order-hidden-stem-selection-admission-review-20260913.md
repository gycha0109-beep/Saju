# General Natal Gyeokguk — Month-Order Hidden-Stem Selection Admission Review

Date: 2026-09-13  
Issue: #509  
Status: research-only partial-admission / generalized-predicate non-admission review  
Production authority: BLOCKED

## Purpose

The remaining General Natal Gyeokguk frontier includes:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
```

Merged research already knows that month order is a primary organizing context and that month-order contents may vary, but it intentionally does not turn canonical hidden-stem storage order into semantic rank.

This review asks a narrower question:

> Does the currently governed `子平真詮 / 子平真詮評注` source material authorize a generalized executable selector that chooses the relevant hidden stem for every month branch?

The answer is **not yet**.

However, the direct source does provide stronger exact evidence than bare membership. That exact evidence is retained rather than discarded.

---

## Upstream authority retained exactly

### #436 — candidate source frontier

Merged #436 establishes:

```text
month order = primary organizing context
month-order contents may change selection
visible-stem transparency = source-observed selection axis
branch meeting = source-observed selection axis
plural/coexisting selections = source-observed
candidate selection != establishment judgment
```

It also freezes:

```text
hidden-stem storage order != semantic rank
candidateDerivationAuthorized = false
establishmentPredicateAuthorized = false
```

The five coarse predicate gaps remain open.

### #441 — canonical month-order observation

Merged #441 permits only:

```text
derivedFacts.hiddenStems.month membership enumeration
exact visible heavenly-stem position observation
```

It does not authorize:

```text
hidden-stem rank
本氣 / 中氣 / 餘氣 mapping
strength
month-command duration
selection winner
candidate identity
establishment verdict
```

This review chains to both upstream definition hashes rather than rebuilding those contracts.

---

## Direct source evidence reviewed

Selected direct source section:

```text
子平真詮評注 / 論用神變化
```

### 1. Month-order plurality

The source states:

```text
月令所藏不一，而用神遂有變化
```

This directly supports the already-governed boundary that a month branch may contain more than one relevant hidden content and that selection/use may vary.

It does not itself provide a complete algorithm for every branch.

### 2. Exact 寅 primary-role wording

The source then gives an exact 寅 example:

```text
即以寅論，甲為本主，如郡之有府，丙其長生，如郡之有同知，戊亦長生，如郡之有通判
```

The review therefore records:

```text
DIRECT_SOURCE_YIN_EXACT_PRIMARY_ROLE = OBSERVED
```

Specifically, within this exact source discussion:

```text
寅 -> 甲 is explicitly called 本主
```

This is stronger than an unordered membership set.

But it is not a repository-wide statement that:

```text
hiddenStems.month[0] = 本主
```

and it is not an explicit all-twelve-branch primary-hidden-stem table.

### 3. Exact 寅 transparency substitution

The source continues:

```text
假使寅月為提，不透甲而透丙，則如知府不臨郡，而同知得以作主
```

The review therefore records:

```text
DIRECT_SOURCE_YIN_EXACT_TRANSPARENCY_SUBSTITUTION = OBSERVED
```

For this exact example:

```text
month branch = 寅
甲 = source-named 本主
甲 not transparent
丙 transparent
=> 丙 may 作主
```

This is a genuine source-semantic relation.

It is preserved as exact source evidence.

---

## Why the exact 寅 evidence does not yet authorize a generalized selector

A production-capable canonical selector would need to answer, for every relevant chart:

```text
1. which hidden stem is the primary month-order content?
2. whether that primary relation is universal across all month branches;
3. what happens when the primary hidden stem is not transparent;
4. which alternate hidden stem may substitute;
5. what happens when multiple hidden stems are transparent;
6. how branch meeting interacts with the hidden-stem choice;
7. how conflicts / precedence / coexisting uses are represented;
8. whether any default applies when no source-governed exact condition matches.
```

The current direct source material reviewed here does not provide a complete executable mapping and settlement contract for all of those questions.

Accordingly:

```text
ALL_BRANCH_PRIMARY_HIDDEN_STEM_MAPPING = UNRESOLVED
GENERALIZED_MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE = UNAUTHORIZED
```

---

## Storage order remains non-semantic

The canonical calculation layer already contains:

```text
derivedFacts.hiddenStems.month
```

That array is valid calculation substrate.

But this review does **not** authorize any of the following transformations:

```text
hiddenStems.month[0] -> 本主
hiddenStems.month[0] -> 本氣
hiddenStems.month[1] -> 中氣
hiddenStems.month[2] -> 餘氣
array position -> strength
array position -> month-command duration
array position -> selection precedence
array position -> candidate rank
```

Those would manufacture semantic authority from storage representation.

Therefore:

```text
HIDDEN_STEM_STORAGE_ORDER_RANKING = UNAUTHORIZED
MAIN_SECONDARY_RESIDUAL_MAPPING = UNAUTHORIZED
MONTH_COMMAND_DURATION = UNAUTHORIZED
```

---

## Exact source evidence is not discarded

Fail-closed does not mean flattening the source into “nothing is known.”

The following remain affirmative observations:

```text
MONTH_ORDER_HIDDEN_STEM_MEMBERSHIP = AUTHORIZED_OBSERVATION_ONLY
DIRECT_SOURCE_MONTH_ORDER_PLURALITY = OBSERVED
DIRECT_SOURCE_YIN_EXACT_PRIMARY_ROLE = OBSERVED
DIRECT_SOURCE_YIN_EXACT_TRANSPARENCY_SUBSTITUTION = OBSERVED
```

The non-admission applies only to the stronger generalized rule:

```text
exact 寅 evidence
!= all-branch primary-hidden-stem mapping
!= generalized hidden-stem selector
!= GEJU_CANDIDATE
!= GEJU_ESTABLISHMENT_STATE
```

---

## Separation from the transparency frontier

The exact source substitution example depends on transparency wording.

That does not bypass the separate transparency authority frontier.

Merged #503 still requires:

```text
day-slot admissibility = UNRESOLVED
slot-set exhaustiveness = UNRESOLVED
full generalized transparency predicate = UNAUTHORIZED
```

This review therefore does not create a complete transparency predicate as a side effect of the 寅 example.

---

## Separation from branch-meeting effect

The source also uses branch meeting as a selection/change axis elsewhere.

Merged #505 still requires:

```text
post-interaction effective bureau = UNRESOLVED
branch-meeting selection effect = UNAUTHORIZED
```

This review does not use the hidden-stem frontier to bypass that independent blocker.

---

## Admission decision

Permanent decision:

```text
DIRECT_SOURCE_EXACT_YIN_PRIMARY_AND_TRANSPARENCY_SUBSTITUTION_OBSERVED_GENERALIZED_ALL_BRANCH_SELECTOR_NOT_ESTABLISHED
```

Exact authority state:

```text
MONTH_ORDER_HIDDEN_STEM_MEMBERSHIP                  = AUTHORIZED_OBSERVATION_ONLY
DIRECT_SOURCE_MONTH_ORDER_PLURALITY                 = OBSERVED
DIRECT_SOURCE_YIN_EXACT_PRIMARY_ROLE                = OBSERVED
DIRECT_SOURCE_YIN_EXACT_TRANSPARENCY_SUBSTITUTION   = OBSERVED
DIRECT_SOURCE_YIN_ROLE_GENERALIZED_BEYOND_YIN       = false

HIDDEN_STEM_STORAGE_ORDER_RANKING                   = UNAUTHORIZED
MAIN_SECONDARY_RESIDUAL_MAPPING                     = UNAUTHORIZED
MONTH_COMMAND_DURATION                              = UNAUTHORIZED
ALL_BRANCH_PRIMARY_HIDDEN_STEM_MAPPING              = UNRESOLVED
GENERALIZED_MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE = UNAUTHORIZED
```

This is a partial-admission / generalized-predicate non-admission verdict.

---

## Executable review contract

The permanent TypeScript review chains to:

```text
#436 GENERAL_NATAL_GEJU_CANDIDATE_SOURCE_FRONTIER_VERSION
#436 GENERAL_NATAL_GEJU_CANDIDATE_SOURCE_FRONTIER_DEFINITION_HASH
#441 GENERAL_NATAL_GEJU_MONTH_ORDER_TRANSPARENCY_OBSERVATION_VERSION
#441 GENERAL_NATAL_GEJU_MONTH_ORDER_TRANSPARENCY_OBSERVATION_DEFINITION_HASH
```

It records four evidence layers:

1. inherited month-order plurality;
2. inherited canonical hidden-stem membership observation;
3. direct exact 寅 `甲為本主` evidence;
4. direct exact 寅 `不透甲而透丙 ... 丙得以作主` evidence.

Every evidence layer keeps:

```text
hiddenStemStorageOrderRankingAuthorized = false
allBranchPrimaryHiddenStemMappingAuthorized = false
generalizedSelectionPredicateAuthorized = false
```

The review is deterministic and content-addressed.

---

## Coarse Gyeokguk gaps

All five coarse gaps remain open:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The first gap is now more precisely characterized:

```text
canonical hidden-stem membership = available
exact 寅 primary-role evidence = available
exact 寅 transparency-substitution evidence = available
all-branch primary mapping = missing
generalized selection procedure = missing
```

---

## Product / Commerce invariant

```text
GEJU_CANDIDATE                   = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE         = NOT_EMITTED
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03                         = OPEN
NEXT_PRODUCTION_SKU              = NONE
Commerce                         = HOLD
```

ProductHost, Character, narrative, LLM, API/browser, SKU, pricing, payment, entitlement, refund, or Commerce layers may not infer the missing selector.

---

## Next honest frontier

This review closes one question:

> Can the currently governed `論用神變化` material be converted directly into a generalized all-branch hidden-stem selector?

No.

A future authority-producing step must obtain direct governed evidence that either:

1. explicitly defines primary/alternate hidden-stem mapping and selection behavior across the required month branches; or
2. supplies another source-native executable rule sufficient to construct the missing selector without relying on repository storage order or unsourced convention.

Until then:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING = OPEN
```
