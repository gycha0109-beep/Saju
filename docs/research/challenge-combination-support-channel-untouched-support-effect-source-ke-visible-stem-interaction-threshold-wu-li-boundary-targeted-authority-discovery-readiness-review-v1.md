# I121 — Source 克 Visible-Stem Interaction Threshold 无力 Boundary Targeted Authority Discovery Readiness Review

## Status

STRICT CLOSED

## Exact verified implementation

- code/test/export HEAD: `4de7593ba929f89d4b4647e39ae7ac971f51653a`
- CI: `#920` — SUCCESS
- verification: 179 test files / 1078 tests
- I121 tests: 8/8
- lint/typecheck/test/build: PASS

## Decision

`WU_LI_BOUNDARY_TARGETED_DISCOVERY_AUTHORIZED_SINGLE_CANDIDATE_FULL_SIX_CONTRACT_PRESERVED_NO_CROSS_SOURCE_EQUIVALENCE`

## Targeted frontier

I120 narrowed the strongest inspected candidate deficit to exactly:

`WU_LI_BOUNDARY_SEMANTICS_AND_EXCEPTIONS`

I121 therefore authorizes only targeted discovery for the semantic boundary of visible-heavenly-stem `无力 / 無力` in a `克` context.

The governing question is:

> Within one exact visible-heavenly-stem 克 authority candidate, what does 无力/無力 explicitly mean for effective interaction, and under what source-local conditions or exceptions does that boundary apply?

## Required literal/context boundary

```text
targetLiteralForms = 无力 / 無力
visibleStemKeContextRequired = true
literalWuLiOrOrthographicEquivalentRequired = true
explicitSemanticBoundaryStatementRequired = true
explicitNoInteractionMeaningIfClaimedMustBeSourceLocal = true
explicitNonZeroOrResidualForceMeaningIfClaimedMustBeSourceLocal = true
explicitExceptionConditionsRequiredIfBoundaryIsConditional = true
```

Simplified `无力` and traditional `無力` may be treated as orthographic forms of the same literal token.

Broader semantic synonyms are not substitutes.

## I119 single-candidate contract remains binding

Targeted discovery does not weaken the six-requirement admission rule:

```text
sameCandidateMustExposeAllSixI118SemanticLociBeforeRegistration = true
targetedRequirementHitAloneMayAuthorizeRegistration = false
targetedRequirementHitAloneMayAuthorizeThreshold = false
candidateMayProceedToCoverageEvaluationOnlyAfterFullSixLocatorAdmission = true
crossCandidateCompositionAuthorized = false
partialCandidateCompositionAuthorized = false
```

Finding an isolated `无力` explanation in a new source is insufficient unless that same candidate also exposes the other five I118 semantic loci required by I119.

## Forbidden semantic mapping

Without an explicit same-source statement, none of the following may be equated with `无力`:

```text
不能相克
不以克论
不作克论
zero effect
negligible effect
residual force
```

Therefore the current observations remain distinct:

```text
陈园: 远干无力
韦千里: 地位远隔，不能相克
```

I121 does not authorize:

```text
无力 = 不能相克
```

across those sources.

## Reinspection boundary

A verified text of the same 韦千里 work may be reinspected for `无力 / 無力`.

Text from another edition or transcription may not be borrowed into the current candidate until exact source identity and reproducibility are resolved.

```text
weiQianliCandidateMayBeReinspectedForWuLiInVerifiedSameWorkText = true
alternateEditionOrTranscriptionTextMayBeBorrowedWithoutIdentityResolution = false
strongestRejectedCandidateMayBePromotedByThisGate = false
```

## No authority created

```text
candidateDiscoveryPerformedByThisGate = false
candidateRegisteredByThisGate = false
requirementCoverageEvaluatedByThisGate = false
authorityAcquiredByThisGate = false
visibleStemBinaryEffectiveInteractionEligibilityResolved = false
effectiveInteractionSetResolved = false
thresholdRuleCreatedByThisGate = false
damageEvaluationAuthorized = false
i98KeDamageVocabularyEvaluationResolved = false
i98ResearchMethodologyMaterializationAuthorized = false
```

Separate hidden-stem blocker remains:

`SOURCE_KE_HIDDEN_STEM_INTERACTION_ELIGIBILITY_AUTHORITY_NOT_ESTABLISHED`

Global settlement, scoring, classification, and production interpretation guards remain closed.

## Next gate

`UNTOUCHED_SUPPORT_EFFECT_SOURCE_KE_VISIBLE_STEM_INTERACTION_THRESHOLD_WU_LI_BOUNDARY_TARGETED_AUTHORITY_DISCOVERY_EVIDENCE`

I122 may perform the actual targeted source search and inspection. It must record exact source identity and reproducible locators, preserve the single-candidate/full-six admission contract, and fail closed if no candidate explicitly supplies the missing same-source `无力 / 無力` boundary together with the remaining required loci.
