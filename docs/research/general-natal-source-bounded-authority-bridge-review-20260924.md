# Saju Bridge — General Natal Source-Bounded Admission Review

Issue: #1447

Watchtower-Track: saju-bridge

## Purpose

This is the first explicit Research ↔ Engine Authority Bridge disposition for the current source-bounded General Natal candidate.

The reviewed candidate is:

```text
src/research/general-natal-conclusion-source-bounded-candidate.ts
version = 0.2.0-research
```

The review does not create a new interpretation engine, admission registry, methodology, rule, claim, reviewer identity, trust grant, or Production authority.

## Existing authority reused

The review consumes existing repository authority instead of duplicating it:

```text
source-bounded candidate registry
→ exact 11-subject domain-review manifest
→ current scan-backed witness qualification
→ Samyeong volume-7 peer-taxonomy scan evidence
→ existing external ReviewAttestation intake / trust-grant-request path
→ Bridge disposition
```

Current candidate shape:

```text
1 methodology
5 T5 source-bounded family-presence rules
5 T8 source-bounded structural-relation rules
= 11 content-addressed review subjects
```

## Current positive findings

The candidate is structurally bounded.

It does not authorize consumer projection from the five structural relations, does not assign fortune polarity or numeric weight, and does not infer personality, work, money, relationship, event, or timing semantics.

The registry is deterministic and the exact review subjects are already content-addressed.

Therefore this review does **not** reject the candidate.

## Current blocking findings

### 1. Source integrity is incomplete

The current scan-backed evidence mesh still records:

```text
sourceIntegrityQualificationEstablished = false
productionEligibleProvenanceEstablished = false
provenanceQualityPromotionAuthorized = false
```

The latest fixed-witness artifact has scan-backed corroboration but does not establish full exact scan/transcription identity for every witness.

The Samyeong volume-7 peer-taxonomy source likewise has:

```text
sameEditionScanAuthorityLocated = true
sameEditionScanOcrPropositionCorroborated = true

exactPhysicalPageOrFolioVerified = false
directScanImageComparisonCompleted = false
exactWitnessHashReproducedFromScan = false
exactTranscriptionIdentityEstablished = false
fullSourceIntegrityQualificationEstablished = false
```

OCR or source count is not promoted into Production provenance automatically.

### 2. Domain review authority is absent

The exact 11-subject review manifest exists, but the default source-bounded registry still contains zero ReviewAttestations.

The repository already has an intake path for externally supplied attestations. This Bridge review does not fabricate one.

### 3. Reviewer trust authority is absent

The repository already has a trust-grant request manifest path, but no actual reviewer trust grant is created by that path.

A ReviewAttestation and a trusted ReviewAttestation are distinct authorities.

### 4. Lifecycle and provenance remain research-only

Current source-bounded rules remain:

```text
status = research
provenanceQuality = secondary_only
methodologyStability = contested
reviewerStatus = unreviewed
```

These fields are not changed merely to satisfy downstream execution gates.

## Bridge disposition

```text
BridgeDecision = RETURN_TO_RESEARCH
Rejected = false

EngineAuthorityPromotion = false
PreviewExpansion = false
OfficialReadingAuthority = false
ProductionAdmission = false
Production = HOLD
```

RETURN_TO_RESEARCH is used because the candidate's exact source-integrity prerequisites are not yet sufficient for governed engine-authority promotion.

This is not a semantic rejection. The bounded candidate may remain available in its current research-only scope.

## Required Research return

Before Bridge re-review, Research must close or explicitly resolve the following:

1. complete exact source-integrity qualification for every source consumed by the current candidate;
2. close remaining fixed-witness scan/transcription identity gaps without treating OCR corroboration as identity;
3. directly verify the Samyeong volume-7 peer-taxonomy scan surface through a governed evidence path;
4. reproduce scan-verified witness digests only when exact scan/transcription identity is established;
5. keep provenance quality unchanged until a separate governed provenance review authorizes a change.

## Governance work that remains after Research

Even if the source-integrity gap closes, Production promotion is not automatic.

Separate governance still requires:

1. real domain ReviewAttestations bound to the exact current 11 content-addressed subjects;
2. independently authorized ReviewerTrustGrants for the exact attestation hashes;
3. a governed provenance-quality decision;
4. a governed lifecycle promotion decision.

These are not Research substitutes and are not inferred by this Bridge review.

## Prohibited extensions

This review does not authorize:

- a new admission framework;
- generic Ten-God relation resolution;
- consumer prose or modern life-domain conclusions from the five structural relations;
- automatic secondary_only to multi_source_supported promotion;
- automatic research to reviewed/active promotion;
- Preview expansion;
- Official Reading activation;
- Production pack activation;
- Gyeokguk, 強弱, 旺衰, Yongshin, timing, SKU, or Commerce expansion.

## Re-review trigger

Re-run this Bridge review only after the source-integrity evidence materially changes or the exact candidate content changes.

A later review must bind the then-current content-addressed review subjects rather than reusing stale subject hashes.
