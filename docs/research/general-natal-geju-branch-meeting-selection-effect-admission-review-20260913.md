# General Natal Gyeokguk — Branch-Meeting Selection-Effect Admission Review

Date: 2026-09-13  
Issue: #504  
Status: research-only non-admission review  
Production authority: BLOCKED

## Purpose

The General Natal mixed-qi research chain already has substantial source-backed branch-meeting evidence.

Merged authority includes:

- #447 — exact source-aligned full-three `會支` structural evidence;
- #451 — transparency + meeting co-use and exact direct 有情 / 無情 exemplars;
- #455 — governed zero/one/multiple selection-signal observation;
- #469 — exact direct source-semantic use identity observation.

The remaining coarse gap is:

```text
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
```

This review asks whether the currently governed evidence is sufficient to promote:

```text
source-aligned branch meeting
-> post-interaction effective bureau
-> generalized branch-meeting selection effect
```

The answer is **no** under the current authority chain.

The result is a non-admission boundary, not a statement that branch meetings have no semantic effect.

---

## Upstream authority retained exactly

### #447 — source-aligned structural meeting evidence

The selected `子平真詮 / 子平真詮評注` mixed-qi discussion supplies exact branch-meeting examples:

```text
辰 + 申 + 子
丑 + 巳 + 酉
未 + 亥 + 卯
戌 + 寅 + 午
```

with direct wording including:

```text
逢申與子會局，則用水印是也
巳酉會成金局
地支會亥卯以成傷官之局
月支又會寅會午以成傷官之局
```

#447 already authorizes the narrow bridge:

```text
canonical full-three structural match
-> source-aligned 會支 evidence
```

but requires the canonical structural relation to remain:

```text
structuralMatchOnly       = true
transformationEstablished = false
```

This review preserves that boundary exactly.

### #451 — co-use and exact affinity evidence

The selected source also states:

```text
一透則一用，兼透則兼用，透而又會，則透與會並用
```

and directly classifies exact cited combinations as 有情 / 無情.

This proves that:

```text
meeting evidence is semantically relevant
meeting and transparency may coexist
interaction quality matters
```

It does **not** provide a chart-general predicate that settles every clash, damage, rescue, competition, or survival question.

### #469 — exact source-semantic use identity

Direct exemplars additionally prove that a branch-meeting signal can carry a source-named semantic use.

Examples already governed include:

```text
辰申子 meeting -> 水印 / 印 axis
未亥卯 meeting -> 傷官
戌寅午 meeting -> 傷官
```

This is source-direct semantic-use identity observation.

It is not post-interaction effective-bureau authority and is not `GEJU_CANDIDATE` identity.

---

## What the source wording proves

The governed source wording is stronger than bare branch membership.

It proves exact-example semantic relevance through expressions such as:

```text
會局
會成金局
以成傷官之局
則用水印是也
```

Therefore this review records:

```text
DIRECT_SOURCE_MEETING_RESULT_WORDING = OBSERVED
DIRECT_SOURCE_MEETING_USE_IDENTITY    = OBSERVED
SOURCE_PLURAL_CO_USE_BOUNDARY         = OBSERVED
DIRECT_AFFINITY_EXEMPLAR_EVIDENCE     = OBSERVED
```

These observations remain valid.

---

## Why generalized post-interaction effect is not admitted

A canonical production predicate would need to answer a stronger question than the direct exemplars answer:

```text
Given a structural meeting in an arbitrary chart,
does that meeting remain effective after all relevant competing interactions?
```

The current governed evidence does not provide a complete executable settlement contract for:

```text
clash survival / breakage
harm or damage settlement
other competing branch interactions
interaction ordering / precedence
rescue or restoration conditions
meeting-vs-transparency precedence
whether absence of a known conflict proves effectiveness
```

Accordingly:

```text
POST_INTERACTION_EFFECTIVE_BUREAU = UNRESOLVED
```

No default positive or negative rule is created.

---

## `會局` / `成局` is not silently retyped as canonical transformation

The source can directly describe an exact exemplar as `會局` or `成局`.

That does not authorize this repository transformation:

```text
source says 會局 / 成局
-> canonical transformationEstablished = true
```

because the canonical structural contract deliberately separates:

```text
structural membership
from
post-interaction transformation/effectiveness
```

Therefore the permanent review keeps:

```text
CANONICAL_TRANSFORMATION_ESTABLISHED = false
```

for the current governed branch-meeting bridge.

This does not contradict the source wording. It prevents an exact source illustration from becoming an unstated chart-general runtime rule.

---

## Exact exemplar semantics are retained, not generalized

The source-direct exemplar chain remains useful.

For example, the governed exact examples can state that:

```text
甲 / 辰 / 癸 + 辰申子
-> exact source use on 印 axis
-> exact source 有情 evidence

壬 / 未 / 己 + 未亥卯
-> exact source uses 官 + 傷官
-> exact source 無情 evidence

甲 / 戌 / 辛丁 + 戌寅午
-> exact source uses 官 + 傷官
-> exact source 無情 evidence
```

But:

```text
exact source exemplar
!= generalized interaction-survival predicate
```

and:

```text
source semantic use identity
!= post-interaction effective bureau
!= canonical candidate identity
!= establishment verdict
```

---

## Admission decision

```text
DIRECT_SOURCE_MEETING_RELEVANCE_OBSERVED_GENERALIZED_POST_INTERACTION_EFFECT_PREDICATE_NOT_ESTABLISHED
```

Exact authority state:

```text
SOURCE_ALIGNED_BRANCH_MEETING_STRUCTURAL_EVIDENCE = AUTHORIZED
DIRECT_SOURCE_MEETING_RESULT_WORDING               = OBSERVED
SOURCE_PLURAL_CO_USE_BOUNDARY                       = OBSERVED
DIRECT_SOURCE_AFFINITY_EXEMPLAR_EVIDENCE            = OBSERVED
DIRECT_SOURCE_MEETING_USE_IDENTITY                  = OBSERVED

CANONICAL_TRANSFORMATION_ESTABLISHED                = false
POST_INTERACTION_EFFECTIVE_BUREAU                   = UNRESOLVED
CLASH_DAMAGE_SETTLEMENT                             = UNAUTHORIZED
COMPETING_INTERACTION_SETTLEMENT                    = UNAUTHORIZED
GENERALIZED_MEETING_SURVIVAL_PREDICATE              = UNAUTHORIZED
BRANCH_MEETING_SELECTION_EFFECT                     = UNAUTHORIZED
```

This is a fail-closed non-admission verdict.

---

## Executable review contract

The permanent TypeScript review chains directly to the merged definition hashes of:

```text
#447 branch-meeting source evidence
#451 co-use / affinity source evidence
#469 source-semantic use identity
```

It inventories three evidence layers:

1. direct source-aligned meeting-result wording;
2. source plural co-use plus exact affinity/disaffinity wording;
3. direct source-semantic use identity carried by meeting signals.

Every layer explicitly keeps:

```text
canonicalTransformationEstablished      = false
generalizedPostInteractionEffectAuthorized = false
```

The review is content-addressed and deterministic.

---

## Coarse Gyeokguk gaps

All five coarse gaps remain open:

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The branch-meeting gap is now better characterized:

```text
structural evidence = available
exact source semantics = available
generalized interaction-survival/effect predicate = missing
```

---

## Product / Commerce invariant

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03                           = OPEN
NEXT_PRODUCTION_SKU                = NONE
GEJU_CANDIDATE                     = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE           = NOT_EMITTED
Commerce                           = HOLD
```

ProductHost, Character, LLM, API/browser, narrative, SKU, pricing, payment, entitlement, refund, or other product layers may not infer the missing post-interaction effect.

---

## Next honest frontier

Current evidence is sufficient to close the question:

> Can the existing branch-meeting source corpus itself be promoted into a generalized post-interaction selection-effect predicate?

No.

The next productive work must therefore be one of:

1. acquire stronger direct authority that explicitly governs interaction survival/effectiveness for branch meetings; or
2. leave this coarse gap open and move to another independently source-governed blocker, especially the still-underdeveloped month-order hidden-stem selection predicate.

Until new direct authority exists:

```text
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING = OPEN
```
