# Relationship / Spouse T8 — 천명관 2026 연애운 direct-body boundary

## Disposition

```text
DIRECT_BODY_POSITIVE_MODERN_FLEXIBLE_INTERPRETATION_NEGATIVE_COMPLETE_ROLE_NEUTRAL_SELECTOR
```

This record freezes the directly traversed complete public HTML body for:

```text
publisher / site = 천명관(天命館) / 준비스튜디오
editorial author = 천명관 편집팀
title = 사주로 보는 연애운
subtitle = 배우자성·배우자궁·도화살로 읽는 인연의 흐름
last updated = 2026-04-08
public URL = https://joonbi.co.kr/guide/saju-love
source class = current public editorial / commercial guide
frontier issue = #534
```

The source is useful because it places a modern-flexibility statement and an operational spouse-reading procedure in the same current public body. Direct review shows that the modern language does not replace the operative sex-conditioned spouse-star selector.

## Direct-body acquisition

The public article is directly retrievable as complete HTML. The complete visible body was traversed as 124 lines:

```text
observed line range = 0-123
observed line count = 124
completeDirectHtmlBodyAcquired = true
fullPageDirectTraversalPerformed = true
directBodySemanticReviewPerformed = true
bodyLevelAdmissionDecisionMade = true
```

No PDF acquisition is involved.

No source excerpt, metadata page, search snippet, commercial preview, or generated summary is substituted for the body.

## Exact body map

```text
identity / update metadata            = lines 2-6
spouse-star rule                      = lines 7-14
spouse-palace rule                    = lines 15-36
relationship timing                   = lines 49-57
single-native vs compatibility scope  = lines 82-94
publisher / operator identity         = lines 101-121
```

## 1. Spouse-star selector retains native-sex branching

The page begins its operational love/marriage section with the traditional spouse-star mapping:

```text
male native   -> Wealth / 財星 -> wife / lover
female native -> Officer / 官星 -> husband / lover
```

The page explicitly acknowledges that this split reflects traditional social-role assumptions. It also states that modern interpretation increasingly considers Wealth and Officer more flexibly as two directions of relationship interaction.

That modern-flexibility statement is preserved as positive current-methodology evidence.

It is not treated as a replacement selector because the same section's own summary still keeps:

```text
남성은 재성
여성은 관성
```

Therefore:

```text
modernFlexibleInterpretationObserved = true
traditionalMaleWealthFemaleOfficerSelectorExplicit = true
sexConditionedSelectorReplacedByCompleteNeutralFormula = false
```

## 2. Relationship timing proves the branch remains operational

The sex distinction is not merely historical background language. The relationship-timing section uses the same branch procedurally:

```text
male native   -> Wealth luck / 財星運
female native -> Officer luck / 官星運
```

The source then uses spouse-star arrival in major or annual luck as a timing signal for meeting, confession, and relationship activation.

Thus:

```text
maleWealthLuckFemaleOfficerLuckTimingBranchPreserved = true
sexConditionedTimingBranchStillOperational = true
```

The page therefore does not publish one native-sex-independent spouse selector and then merely explain an older alternative. Its current operative procedure still depends on native sex.

## 3. Day Branch spouse palace is a real but positional layer

The same page defines the spouse palace as the Day Branch (`일지 / 日支`). It treats spouse star and spouse palace as different analytical layers:

- spouse star: what sort of person / relationship signal is being read;
- spouse palace: how married life or the closest shared-living relationship is situated.

The Day Branch rule is sex-common and operational. It is therefore a genuine role-neutral positional layer.

But it does not replace the spouse-star branch. The page itself continues to use male-Wealth / female-Officer for spouse-star selection and timing.

Accordingly:

```text
dayBranchDefinedAsSpousePalace = true
spousePalaceIsSexCommonPositionalLayer = true
spousePalaceReplacesSexConditionedSpouseStarSelector = false
```

`Day Branch = spouse palace` therefore cannot close the complete `EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING` gap by itself.

## 4. Single-native natal reading is distinguished from compatibility

The article explicitly distinguishes:

```text
연애운 = one person's natal chart -> relationship tendency and timing
궁합   = two people's charts -> comparative harmony
```

This is useful because it shows that the spouse-star and timing rules in this page are genuinely intended for a one-person natal reading rather than being only a two-chart compatibility method.

However, the single-native input boundary does not cure the sex-conditioned selector. The page still lacks one complete native-sex-independent and partner-sex-independent spouse mapping.

## 5. Semantic decision

The frozen source-bounded disposition is:

```text
DIRECT_BODY_POSITIVE_MODERN_FLEXIBLE_INTERPRETATION_NEGATIVE_COMPLETE_ROLE_NEUTRAL_SELECTOR
```

Positive findings:

```text
complete public body directly inspected                = YES
modern reinterpretation / flexibility acknowledged     = YES
Day Branch spouse-palace positional layer               = YES
one-person love reading distinguished from compatibility = YES
```

Blocking findings:

```text
male native -> Wealth spouse-star branch survives       = YES
female native -> Officer spouse-star branch survives    = YES
male Wealth-luck / female Officer-luck timing survives  = YES
complete native-sex-independent spouse selector         = NO
complete partner-sex-independent spouse selector        = NO
```

The source is current and operational enough to prove the negative boundary, but it does not close the missing role-neutral selector authority.

## Safety and provenance boundary

No action in this review performs or authorizes:

```text
opaque identifier guessing
login/session bypass
institution-auth bypass
paywall bypass
DRM/decryption
TLS verification disable
```

The source is ordinary public HTML and requires none of those actions.

## No cross-source stitching

This source is adjudicated on its own body.

Its modern-flexibility language is not combined with OpenFate's role-policy language, Kweon Sujeong's same-sex-family discussion, Hong Yooseon's role/ideology reinterpretation, Kim Sanghan's gender-asymmetry scholarship, Lee Ockhwa's Yukchin-variability abstract, Kim Mantae's spouse-palace priority, or any other partial source to manufacture the missing selector.

Likewise, the Day-Branch spouse-palace rule is not combined with another source's role-neutral language to create a synthetic rule that this source itself does not publish.

## Authority ledger

Unchanged:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 2/5
authorityGapsOpen = 3/5
authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

No spouse producer, rule, claim type, interpretation pack, consumer narrative, preview default, compatibility activation, or production promotion is authorized by this evidence.

## Next authority action

Continue prioritizing a **single source with a complete public body** that itself provides one operational spouse selector satisfying all of:

```text
single-source
single-native
natal-facts-only
operational / executable
native-sex-independent
partner-sex-independent
```

The decisive requirement is a source-published replacement mapping or selector, not merely modern-flexibility language, a role-neutral positional palace, a compatibility method, or a prohibition on inferring sex/orientation.
