# I254 — Qianli season/plurality primary-page binding hold

Status: research-only hold record  
Date: 2026-08-26  
Target: corrected 1936 `韋千里命學講義` NLC witness `nlc:data_416,01jh000368,10155`.

## 1. Purpose

I253 corrected the Qianli bibliographic binding and directly rebound three classes on printed p.14:

- `明暗`;
- `地位`;
- relation-local `支冲` hidden-stem interaction.

It intentionally held the continuation `本氣 / 時令 / 多寡` because the continuation page had not been directly inspected. I254 performs only that missing-page acquisition attempt.

## 2. Direct primary access result

A fresh access to the 1936 Wikimedia/NLC PDF succeeded for zero-based PDF page 300 / one-based PDF page 301, printed p.14. The rendered page again visibly contains the `支冲` opening after `明暗`, `地位`, and `歲運`.

Repeated attempts to render the immediate continuation at zero-based PDF page 301 / one-based PDF page 302 failed with a primary-PDF page cache miss. Neighboring continuation attempts also failed during the gate.

Therefore no new primary page is bound by I254.

## 3. Transcription evidence is not promoted

A public transcription surface contains the exact continuation sequence:

- `本氣`;
- `時令`;
- `多寡`.

It also places those terms directly after `變沖之影響`, which is highly useful for locating the missing primary page. But transcription alignment cannot replace direct inspection of the corrected primary witness.

Derivative mirrors are treated the same way: locator/discovery evidence only, not primary evidence.

## 4. 1934 variant

A separate NLC primary witness for the same work exists:

- `韋千里命學講義`
- 1934
- NLC `nlc:data_416,17jh007058,102955`
- 404 scan pages.

The variant metadata is confirmed, but the exact corresponding continuation page was not directly inspected. It therefore cannot substitute automatically for the unavailable 1936 continuation page.

## 5. Hold decision

Season/plurality remains held under corrected provenance:

- `seasonPluralityPrimaryPageBound = false`
- corrected primary authority unavailable
- historical I252 season/plurality coverage may not simply be carried forward
- numeric season/plurality weights remain forbidden
- winner, damage, destruction, or settlement semantics remain forbidden.

The hold does **not** invalidate:

- the three I253 primary-page-bound p.14 classes;
- the independent printed p.49 `事業` Career-relation candidate.

## 6. Career consequence

B22 classified the seasonal Career modifier path as `CONDITIONAL_IF_DIMENSION_CONSUMED`, not an active primary remediation path.

Accordingly B23 does not need to wait for this hold to clear. It may continue only with season/plurality explicitly excluded from personalized Career T8 consumption.

The conditional seasonal remediation remains inactive.

## 7. Release trigger

Release this hold only after direct inspection of:

1. the exact continuation page in the corrected 1936 NLC witness, or
2. an exact corresponding page in a provenance-equivalent primary edition,

with `本氣 / 時令 / 多寡` visible in page context.

Until then, transcription or derivative mirrors remain locator evidence only.

## 8. No implementation promotion

I254 creates no:

- new rule;
- claim type;
- interpretation pack;
- Career T8 synthesis;
- narrative plan;
- preview switch;
- production policy.

Production impact: **NONE**.

Recommended next gate:

`CAREER_PERSONALIZATION_T8_CURRENT_METHOD_RESIDUAL_AUTHORITY_TARGETED_REMEDIATION_EVIDENCE`
