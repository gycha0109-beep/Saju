# General Natal Gyeokguk — Establishment Source-Clause Admission Review

Date: 2026-09-13  
Issue: #513  
Status: research-only source-clause admission / canonical-execution hold  
Production authority: BLOCKED

## Purpose

This review continues the remaining General Natal Gyeokguk frontier:

```text
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

The question is no longer whether the selected classical source discusses establishment outcomes at all. The direct section `論用神成敗救應` provides explicit success and failure clause families and separately preserves mixed-outcome and rescue semantics.

The narrower question is whether that source authority is sufficient to emit a canonical executable `GEJU_ESTABLISHMENT_STATE` from current repository facts.

The answer is **not yet**.

## Upstream authority

### #436 candidate-source frontier

#436 already records that month-order selection and establishment judgment are distinct semantic stages. It also keeps candidate derivation and establishment execution unauthorized.

This review chains to:

```text
GENERAL_NATAL_GEJU_CANDIDATE_SOURCE_FRONTIER_VERSION
GENERAL_NATAL_GEJU_CANDIDATE_SOURCE_FRONTIER_DEFINITION_HASH
```

### #473 candidate-identity admission review

#473 establishes that source-direct use identity, pattern wording, formation-outcome wording, and primary/co-pattern wording exist, but the repository-required canonical pre-establishment candidate stage is not defined.

This review chains to:

```text
GENERAL_NATAL_GEJU_CANDIDATE_IDENTITY_ADMISSION_REVIEW_VERSION
GENERAL_NATAL_GEJU_CANDIDATE_IDENTITY_ADMISSION_REVIEW_DEFINITION_HASH
```

## Direct source finding

Current direct-source review of `子平真詮評注 / 論用神成敗救應` establishes four source-semantic layers:

```text
formation success clause families
formation failure clause families
mixed outcome layer
rescue / relative-weighting layer
```

The section directly covers eight named pattern-family scopes represented in this review as:

```text
zheng_guan
cai
yin
shi_shen
qi_sha
shang_guan
yang_ren
jian_lu_yue_jie
```

This is materially stronger than isolated `格成` / `不成` exemplars.

Therefore this review admits:

```text
DIRECT_SOURCE_FORMATION_SUCCESS_CLAUSES = OBSERVED
DIRECT_SOURCE_FORMATION_FAILURE_CLAUSES = OBSERVED
DIRECT_SOURCE_MIXED_OUTCOME_AND_RESCUE_LAYER = OBSERVED
SOURCE_SEMANTIC_ESTABLISHMENT_CLAUSE_INVENTORY = AUTHORIZED_RESEARCH_ONLY
SOURCE_PATTERN_FAMILY_SCOPE = OBSERVED
```

## Why canonical execution is still blocked

Source-semantic rule authority and canonical executable input authority are separate.

The current repository still lacks governed resolution for at least:

```text
canonical_candidate_identity
ordinary_strength_classification
root_weight_classification
position_appropriateness
xing_chong_po_hai_effect
mixed_outcome_representation
rescue_precedence_and_weighting
```

The direct source clause families use concepts represented by those unresolved primitives. Their presence in the source does not authorize deriving those primitives from unrelated canonical storage fields.

For example:

```text
resolved Ten-God identity != established candidate identity
hidden-stem array order != root weight
structural relation membership != source-specific effect settlement
raw pillar position != source-authorized position appropriateness
```

## Binary-state boundary

The direct source does not stop at a flat success/failure list. It separately preserves:

```text
success containing a negative factor
failure recovered through rescue
rescue conditions
relative weighting / settlement
```

Therefore this review records:

```text
BINARY_ESTABLISHMENT_STATE_SUFFICIENT = false
```

This does not claim that a future final product can never expose a simple user-facing result. It means the current authority does not permit the internal source-semantic evaluation process to be collapsed into an unqualified boolean without a governed representation and settlement rule.

## Admission decision

```text
SOURCE_CLAUSE_FAMILIES_OBSERVED_CANONICAL_EXECUTION_NOT_AUTHORIZED
```

Exact state:

```text
SOURCE_SEMANTIC_ESTABLISHMENT_CLAUSE_INVENTORY = AUTHORIZED_RESEARCH_ONLY
CANONICAL_CANDIDATE_IDENTITY = UNAVAILABLE
CANONICAL_CLAUSE_INPUT_RESOLUTION = BLOCKED
CANONICAL_EXECUTABLE_ESTABLISHMENT_PREDICATE = UNAUTHORIZED
GEJU_CANDIDATE = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE = NOT_EMITTED
```

## Coarse-gap disposition

This review materially narrows the establishment frontier:

```text
missing direct source success/failure clauses
-> no longer the blocker
```

The remaining blocker is:

```text
source clause inventory
-> canonical candidate identity
-> canonical primitive resolution
-> mixed outcome / rescue representation
-> settlement / precedence
-> executable establishment state
```

The shared coarse-gap constant is intentionally not rewritten in this review. Therefore all five coarse gaps remain listed, including:

```text
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

A later review may revise the shared frontier only after the canonical execution contract is actually authorized.

## Product / Commerce invariant

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
NEXT_PRODUCTION_SKU = NONE
Commerce = HOLD
```

No production runtime, ProductHost, Character, narrative, LLM, API/browser, SKU, payment, entitlement, refund, or Commerce authority is added.

## Next honest frontier

The next establishment-specific authority work should not repeat source discovery for generic success/failure clauses. The source layer is now sufficiently explicit for research inventory purposes.

The next useful work must choose one of these narrower execution blockers:

```text
canonical candidate identity / representation
mixed outcome + rescue representation
one unresolved clause primitive with independent source authority
```

Until one of those advances, a canonical `GEJU_ESTABLISHMENT_STATE` producer remains unauthorized.
