# Relationship / Spouse T8 — Sajunareum Advanced Lesson 17 access boundary

## Disposition

```text
PUBLIC_INDEXED_HIGH_VALUE_ROLE_NEUTRAL_SIGNAL_NO_FULL_BODY_ADMISSION_DECISION
```

This record freezes the current public retrieval boundary for Sajunareum Advanced Lesson 17, `Reading a partner — how to read a connection` / `배우자 통변 — 인연을 읽는 법`.

It is discovery evidence only. It is **not** a body-level selector admission or rejection.

## Source identity

```text
publisher = Sajunareum / 사주나름
lesson = Advanced Lesson 17
English title = Reading a partner — how to read a connection
Korean title = 배우자 통변 — 인연을 읽는 법
English URL = https://sajunareum.com/en/study/adv/17/
Korean URL = https://sajunareum.com/study/adv/17/
source class = current public educational / editorial methodology lesson
frontier issue = #552
```

Fresh duplicate review before opening #552 found no matching Sajunareum issue, PR, commit, or default-branch evidence in `gycha0109-beep/Saju`.

## Why the indexed signal matters

The public search index exposes a stronger role-neutral natal-method signal than a page that merely calls the Day Branch the spouse palace.

The indexed lesson first presents the historical formula:

```text
male -> Wealth as wife
female -> Officer as husband
```

It then explicitly contrasts that with a modern method that centers the Day Branch / spouse seat regardless of gender and treats Wealth and Officer as connection catalysts.

The indexed four-step reading order is:

```text
1. Day Branch Ten God -> partner expectation / partner role
2. natal Day Branch combinations and clashes -> relationship-base structure
3. traditional spouse-star state -> reference layer
4. timing -> years of connection
```

The indexed practice section again builds the report from the Day Branch Ten God, Day Branch combinations/clashes, and timing candidates.

This is important because the first two steps are both single-native and natal-facts-based signals, and step one assigns an explicit partner-role interpretation to a derived natal fact rather than merely naming a chart position.

## Exact access boundary

The Korean and English pages are publicly indexed with target-specific sections, but direct retrieval through the available public browser surface returns `Cache miss`. A separate normal direct download attempt also did not recover the body.

Therefore:

```text
publicSearchIndexRecoveredTargetSpecificContent = true
directPageFetchAttempted = true
directPageFetchDisposition = CACHE_MISS
normalDirectDownloadAttempted = true
normalDirectDownloadDisposition = DOWNLOAD_UNAVAILABLE
completeDirectHtmlBodyAcquired = false
fullPageDirectTraversalPerformed = false
completePdfAcquired = false
renderedPageCount = 0
directBodySemanticReviewPerformed = false
bodyLevelAdmissionDecisionMade = false
```

The indexed sections are deliberately not relabeled as complete direct-body review.

## What remains unresolved

The indexed material is promising but cannot close `EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING` yet.

Complete-body review must answer one precise question: does the same lesson make the Day-Branch-derived procedure independently sufficient for the natal partner selector, with the traditional spouse-star layer genuinely demoted to optional reference/timing context, or does a required sex-conditioned spouse-star dependency survive elsewhere in the complete method?

Until the complete body is directly traversed:

```text
completeRoleNeutralNatalSelectorEstablishedFromIndexedExcerpts = false
completeBodyFreeOfRequiredSexConditionedDependencyEstablished = false
absenceOfSelectorBlockerFromCompleteBodyInferredFromIndexedExcerpts = false
```

No positive admission and no negative rejection is made from partial indexed evidence.

## No cross-source stitching

This record does not combine Sajunareum with InnerCipher, OpenFate, Saju Works, Ming Map, Clarify, or another source to manufacture or reject a selector that Sajunareum itself has not yet been shown to publish in its complete body.

## Authority ledger

Unchanged:

```text
QUALIFYING_PRIMARY_WITNESS = CLOSED
INDEPENDENT_NORMATIVE_PROVENANCE = CLOSED
EXPLICIT_ROLE_NEUTRAL_NATAL_MAPPING = OPEN
CURRENT_GOVERNED_SEMANTIC_CORRESPONDENCE = OPEN
RELATIONSHIP_T6_INPUT = OPEN

authorityGapsClosed = 2/5
authorityGapsOpen = 3/5
authorityAdmissionReady = false
spouseT8ProducerReady = false
productionPromotionReady = false
Production = HOLD
```

No spouse producer, governed semantic mapping, T6 input path, or production activation is authorized by this access-boundary record.

## Upstream chain

This evidence chains from the merged InnerCipher 2026 boundary at PR #546. That upstream boundary already preserves the exact two-of-five ledger and remains the required fail-closed predecessor.

## Next authority action

Priority order:

1. recover the complete Sajunareum Advanced Lesson 17 body through a normal public route and make a source-bounded semantic decision; or
2. continue discovery for a directly traversable single source that itself publishes a selector satisfying:

```text
single-source
single-native
natal-facts-only
operational / executable
native-sex-independent
partner-context-independent
```

until one source closes the mapping without cross-source semantic stitching.
