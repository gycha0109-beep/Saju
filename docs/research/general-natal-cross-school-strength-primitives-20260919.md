# R020 — Cross-school 身強身弱 decision primitives

Date: 2026-09-19  
Issue: #946  
Status: DIVERGENT / TEXTUAL DEPENDENCY RISK OPEN

## 1. 徐樂吾 — 子平真詮評註

Direct surface:

https://www.ncc.com.tw/fate/paleo/bg/bg_032.htm

The commentary explicitly separates:

```text
得時 -> 旺
失時 -> 衰
黨眾 -> 強
助寡 -> 弱
```

and names:

```text
比劫 / 印綬 / 通根扶助
```

as 黨眾-side support families.

It also gives the spring/autumn Wood counterexample pair.

## 2. 任鐵樵 — 滴天髓闡微 / 衰旺

Direct surface:

https://zh.wikisource.org/zh-hant/%E6%BB%B4%E5%A4%A9%E9%AB%93%E9%97%A1%E5%BE%AE

The 任氏 commentary rejects a rigid month-only rule and explicitly requires year/day/hour additions and losses to be considered. It then gives the same broad spring-Wood / autumn-Wood example scaffold and the same heavy/light root comparison family.

## 3. Critical lineage warning

These two texts are **not safe to count as two independent votes**.

The overlap is too strong:

```text
spring Wood counterexample
autumn Wood counterexample
四柱有根
長生祿旺 = heavy root
墓庫餘氣 = light root
比肩 vs 通根 comparison
```

At the same time they are not textually identical.

Material variant:

```text
徐 commentary spring-Wood branch pair: 支酉丑
任 commentary spring-Wood branch pair: 支申酉
```

Therefore:

```text
shared idea = OBSERVED
textual dependency/reuse risk = HIGH
independent corroboration count = NOT SETTLED
exact identity = false
```

This is precisely why a "three schools agree" confidence score would currently be invalid.

## 4. 命理探源

The repository already governs a separate `命理探源` Twelve-Growth table, but R020 requires its **strength chapter**, not that mapping table.

A searchable secondary transcription indicates a chapter titled around `得時不旺失時不弱` and shows substantial conceptual overlap with the 任/Xu family.

That is locator evidence only.

Until a primary/public text surface for that exact strength passage is directly verified:

```text
MINGLI_TANYUAN_STRENGTH_PRIMITIVES = PRIMARY_TEXT_VERIFICATION_OPEN
```

## 5. Current comparison

```text
徐:
  explicit 旺衰 vs 強弱 two-axis semantics
  explicit 黨眾/助寡 vocabulary

任:
  explicit anti-month-only warning
  explicit root correction and root-weight comparisons

shared:
  month command matters but is not sufficient
  roots/support/opposition can overturn a naive seasonal verdict

not established:
  independent textual lineage
  universal cross-school vocabulary identity
  universal threshold
  numeric strength
  majority-vote confidence
```

## 6. Boundary

R020 does not authorize a generalized 身強身弱 classifier.

Its immediate value is methodological: before comparing "schools", textual inheritance must be separated from genuinely independent agreement.

No Production / SKU / Commerce authority is created.
