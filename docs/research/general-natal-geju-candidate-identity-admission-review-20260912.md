# General Natal Gyeokguk — Candidate Identity Admission Review

Date: 2026-09-12  
Status: research-only admission review  
Issue: #472  
Production authority: BLOCKED

## Purpose

This review follows merged #469, which established only exact direct-source **semantic use identity observations**.

Merged authority now permits statements such as:

```text
multiple governed source signals may refer to one directly source-named use
multiple governed source signals may refer to distinct directly source-named uses
signal count and source-semantic-use count are not interchangeable
```

It still explicitly forbids:

```text
source semantic use == GEJU_CANDIDATE
semantic deduplication == candidate deduplication
candidate rank / precedence / strength
establishment success / failure
```

The next authority question is therefore narrow:

> Does the selected source itself define a canonical **pre-establishment candidate identity stage** that can be mapped from the governed semantic-use layer without inventing semantics?

## Source surfaces reviewed

Selected work:

```text
子平真詮 / 子平真詮評注
```

### 1. Source-use selection and plurality

The mixed-qi discussion directly states:

```text
一透則一用，兼透則兼用，透而又會，則透與會並用
```

This supports source-use selection and coexistence.

Merged #469 already demonstrated that the count of these governed source signals is not the count of semantic uses.

This wording does **not** publish a distinct repository-style object equivalent to:

```text
GEJU_CANDIDATE
```

between selection/use observation and later formation judgment.

### 2. Direct pattern wording

The exact 甲生辰月 example states that 癸 transparency together with the 申子辰 meeting is:

```text
印綬之格
```

and simultaneously describes it as:

```text
清而不雜
合而有情
```

The pattern identity is therefore source-direct in this exact exemplar.

However, the same wording is already embedded in a quality/compatibility judgment. It does not independently define a pre-establishment candidate data model.

### 3. Formation-outcome wording

The same mixed-qi chapter later uses language including:

```text
似成格矣
印格不成
```

The selected source therefore uses `格` vocabulary directly while evaluating whether formation appears to occur or fails to occur.

This is decisive for the current architecture boundary:

```text
source pattern word
!= automatically pre-establishment candidate identity
```

Otherwise the repository would collapse candidate identity and establishment outcome back into one semantic layer.

### 4. Primary / co-pattern wording

The `論用神變化` section gives an exact example:

```text
格成正財，正官乃其兼格也
```

This establishes a real source boundary:

```text
one exact source example may directly name a formed primary 格 and a 兼格
```

It does **not** establish a generalized algorithm for:

```text
candidate array order
candidate rank
candidate precedence
candidate strength
single-winner selection
primary-vs-secondary candidate settlement before establishment
```

The phrase itself says `格成`, so using it to manufacture a pre-formation candidate hierarchy would invert the source stage.

## Admission decision

```text
SOURCE_USE_AND_PATTERN_WORDING_OBSERVED_PRE_ESTABLISHMENT_CANONICAL_CANDIDATE_STAGE_NOT_DEFINED
```

### Source authority gained

No new candidate authority is gained.

The review only freezes these source observations:

```text
SOURCE_USE_SELECTION_IDENTITY = OBSERVED
SOURCE_NAMED_PATTERN_WORDING = OBSERVED
SOURCE_FORMATION_OUTCOME_WORDING = OBSERVED
SOURCE_PRIMARY_CO_PATTERN_WORDING = OBSERVED
```

### Candidate bridge not authorized

```text
sourceUseToCanonicalCandidateBridgeAuthorized = false
sourcePatternWordingToPreEstablishmentCandidateBridgeAuthorized = false
canonicalCandidateStageDefinitionAuthorized = false
semanticUseDeduplicationIntoCandidateAuthorized = false
primaryCoPatternGeneralizedPrecedenceAuthorized = false
candidateIdentityAuthorized = false
multipleCandidateRepresentationAuthorized = false
candidateDerivationAuthorized = false
```

The reason is not that the source lacks pattern identities. It clearly names them.

The reason is that the selected source does not publish the **separate intermediate contract required by this architecture**:

```text
source selection/use
-> canonical pre-establishment candidate identity
-> establishment success/failure
```

The repository must not invent the middle arrow merely because both ends exist in the text.

## Establishment remains separate

Current architecture already requires candidate selection and establishment judgment to remain separate.

This review reinforces that boundary:

```text
selectionEstablishmentSeparationStillRequired = true
establishmentPredicateAuthorized = false
establishmentFactsEmitted = false
```

Exact source phrases such as `似成格矣`, `印格不成`, and `格成正財` are evidence that formation semantics exist in the source. They are **not** sufficient to create a generalized canonical establishment predicate.

## Multiple-candidate gap remains OPEN

The merged semantic-use layer proved that plural source uses can exist safely without destructive collapse.

This review additionally proves that source wording may name a primary formed 格 plus `兼格` in an exact example.

Nevertheless:

```text
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
```

remains OPEN because the missing production contract still includes:

```text
what exact object is a candidate before establishment?
how is a source-use identity bridged to that object?
when are same-use observations one candidate or several candidate evidences?
how are source pattern words separated from already-formed pattern verdicts?
how are multiple candidate identities represented before any authorized precedence rule?
```

## Five coarse gaps remain OPEN

```text
MONTH_ORDER_HIDDEN_STEM_SELECTION_PREDICATE_AUTHORITY_MISSING
VISIBLE_STEM_TRANSPARENCY_SELECTION_PREDICATE_AUTHORITY_MISSING
BRANCH_MEETING_SELECTION_EFFECT_AUTHORITY_MISSING
MULTIPLE_GEJU_CANDIDATE_REPRESENTATION_AUTHORITY_MISSING
GEJU_ESTABLISHMENT_SUCCESS_FAILURE_PREDICATE_AUTHORITY_MISSING
```

No coarse gap is closed by this review.

## Product / Commerce invariant

```text
GENERAL_NATAL_PRODUCTION_AUTHORITY = BLOCKED
P0-CM-03 = OPEN
GEJU_CANDIDATE = NOT_EMITTED
GEJU_ESTABLISHMENT_STATE = NOT_EMITTED
Commerce = HOLD
```

ProductHost, Character, LLM, API/browser, SKU, payment, entitlement, and refund layers must not reinterpret source-use IDs, source pattern names, `格成`, or `兼格` as production candidate facts.

## Next honest frontier

Candidate identity cannot currently be promoted by terminology alone.

The next productive research should target one of the actual missing bridges rather than adding another alias layer:

1. a source-backed generalized month-order hidden-stem selection predicate;
2. a complete transparency selection predicate, including unresolved slot admissibility;
3. branch-meeting selection/effect semantics distinct from structural formation;
4. a source that explicitly distinguishes pre-establishment candidate identity from established 格 identity;
5. a generalized establishment success/failure predicate with all required interaction settlement inputs.

Until one of those paths supplies the missing semantics:

```text
candidate identity authority remains BLOCKED
```
