# P4.3B41 — Branch Clash Remediation Trigger Readiness Review

## Status

- Stage: P4.3B41
- Domain: Career
- Scope: natal / research
- Base main: `047e3ed212d73eb07e3909cd03be04ec6187db36`
- Upstream: exact content-addressed B40 Branch adequacy / compatibility review
- Production impact: NONE

## Purpose

Freeze the exact evidence change required before Branch-clash authority research resumes.

B40 established material evidence but also two unresolved admission blockers:

1. `EXACT_2015_PRINTED_TARGET_PASSAGE_BINDING`
2. `CURRENT_METHOD_DEPENDENCY_SEPARABILITY_OR_COMPATIBILITY_EVIDENCE`

B41 does not acquire new evidence and does not admit authority. It prevents repeated broad searching over unchanged surfaces and prevents the attractive same-work semantic mechanism from being promoted while either blocker remains unresolved.

## Trigger contract A — existing 2015 path

`BRANCH_2015_EXACT_EDITION_COMPATIBILITY_TRIGGER`

The existing 《十神闡微》 path may reopen only when **all** of the following are true:

1. the exact 2015 printed target passage is directly inspected,
2. enough exact-edition local method context is inspectable to determine whether distance / strength / 旺衰 is mandatory for the bounded modifier,
3. the same 2015 path itself establishes dependency separability/current-method compatibility,
4. the semantic remains exactly `정관 → TEN_GOD_JEONG_GWAN_FORMAL_RESPONSIBILITY → formal_responsibility` with `ATTENUATES_OR_REDUCES_EXPRESSION` and no base-semantic deletion.

If exact-edition inspection instead confirms that strength/旺衰 is mandatory for the modifier, the correct disposition is:

```text
CURRENT_METHOD_INCOMPATIBLE
```

not forced authority admission.

### Not acceptable substitutes

- the already inspected same-work web/full-text lineage,
- ISBN/catalog metadata without target body,
- silently dropping strength/旺衰,
- borrowing compatibility or limits from another source.

## Trigger contract B — new independent complete path

`BRANCH_INDEPENDENT_COMPLETE_PATH_TRIGGER`

A genuinely new published source path may reopen Branch research only if that **one source path independently** supplies:

- exact 正官 → governed `formal_responsibility` bridge,
- natal branch-clash condition,
- qualitative modifier,
- explicit limits/context,
- current-method compatibility without unsupported strength/旺衰 dependency.

It cannot borrow the 2015 path's semantic mechanism and then use another source only for compatibility or limits. That would be source stitching.

### Not acceptable substitutes

- temporal 運 examples treated as natal,
- generic clash material without the governed current-T5 semantic,
- cross-source stitching,
- snippets / TOC / testimonial / bibliography without normative body.

## Current trigger state

```text
BRANCH_2015_EXACT_EDITION_COMPATIBILITY_TRIGGER  CLOSED
BRANCH_INDEPENDENT_COMPLETE_PATH_TRIGGER         CLOSED

currently satisfied triggers                    0
currently executable Branch lanes                0
```

No broad search restart is authorized merely because these contracts now exist.

## Activation semantics

A future trigger activation:

```text
DOES NOT automatically admit authority
DOES NOT automatically close the historical gap
ALWAYS requires a new adequacy review
```

This preserves the research-state distinction:

```text
DISCOVERED
→ TRIGGER_REOPENED
→ MATERIAL_PARTIAL / ADMISSION_READY
→ AUTHORITY_ADMITTED
→ GAP_CLOSED
```

No stage is skipped.

## Preserved authority state

The already admitted B38 bounded Position component remains unchanged:

```text
Position bounded component authority admitted = 1
Branch authority admitted                     = 0
```

`BRANCH_CLASH_TO_T5_CAREER_SEMANTIC_MODIFIER_MISSING` remains open, and all six historical Career T8 synthesis gaps remain formally open.

## Explicit prohibitions

B41 authorizes no:

- repeated search over unchanged 2015 full-text/catalog surfaces,
- cross-source requirement stitching,
- numeric weighting,
- occupation modernization,
- T5 base mutation,
- T8 rule,
- T8 claim type,
- personalized T8 pack,
- consumer narrative,
- preview default switch,
- production promotion.

## Verification

The implementation freezes:

- 2 remediation requirements,
- 2 trigger contracts,
- 12 governance controls,
- 13 fail-closed regression tests.

## Decision

```text
Branch remediation trigger contracts frozen       2
currently satisfied                                0
currently executable Branch lanes                  0
Branch authority admission-ready                   NO
Branch authority admitted                          NO
historical gap closed                              NO
production impact                                  NONE
```

## Next gate

`CAREER_PERSONALIZATION_T8_POST_B41_RESEARCH_FRONTIER_HOLD_REVIEW`

The next gate should only reconcile whether any remaining authority lane is legitimately executable after B41. It must not manufacture work by restarting blocked broad research. If no lane has a changed evidence surface or already-admission-ready component, the correct result is a governed research hold rather than premature T8 implementation.
