# I0 / I1 Foundation Status

- Date: 2026-08-19
- Branch: `agent/architecture-foundation`
- Status: **STRICT CLOSED**

## I0 — Repository / Tooling Bootstrap

Closed baseline:

- Node.js 24 LTS policy
- npm 11.17.0 package manager baseline
- TypeScript 6.0.2 exact pin
- ESLint 10.8.0
- typescript-eslint 8.66.0
- Prettier 3.9.6
- Vitest 4.1.10
- strict TypeScript compiler options
- build declaration output
- npm exact-save / engine-strict policy
- committed `package-lock.json` (lockfile v3)
- read-only GitHub Actions CI
- locked CI installation through `npm ci`

The lockfile was generated by the same Node 24 / npm 11.17.0 GitHub Actions environment used for verification, committed to the feature branch, and then CI was converted from bootstrap `npm install` to reproducible `npm ci`.

---

## I1 — Domain Contracts

Implemented:

```text
src/contracts/common.ts
src/contracts/calculation.ts
src/contracts/interpretation.ts
src/contracts/narrative.ts
src/contracts/reading.ts
src/contracts/runtime-validation.ts
src/contracts/index.ts
src/index.ts
```

Closed invariants:

- canonical fact state is explicit: `resolved / ambiguous / unavailable`
- ambiguous facts require two or more candidates and a reason
- unavailable facts require a reason
- unknown birth time cannot contain fabricated `hour` / `minute`
- leap month cannot be enabled for solar input
- input time and coordinates are range-validated
- Calculation Policy identifiers and enums are runtime-validated
- external version references have runtime validation
- interpretation and narrative contracts do not collapse into calculation authority

---

## Verification Evidence

### Precheck

Before the hosted toolchain became observable, the contracts passed a strict TypeScript 5.8.3 reconstruction and manual runtime invariant checks. These were prechecks only and are no longer the close authority.

### Hosted CI

GitHub Actions runner observed:

```text
Node.js  v24.19.0
npm      11.17.0
```

Bootstrap validation established that exact dependencies install successfully and generated the committed lockfile.

Final close gate:

```text
CI run:       #60
run id:       32204323510
job id:       95924415712
install:      npm ci --no-audit --no-fund     PASS
lint:         ESLint                           PASS
typecheck:    TypeScript 6.0.2                PASS
test:         Vitest                           PASS
build:        TypeScript build                 PASS
job:                                           SUCCESS
```

Therefore:

```text
I0 = STRICT CLOSED
I1 = STRICT CLOSED
```

---

## Formatting

Prettier remains configured. `format:check` is not yet part of the blocking `npm run check` chain. This is a non-authority tooling enhancement and does not reopen I0/I1 correctness gates.

---

## I2 Authorization

The previous prohibition on production I2 work is lifted.

I2 may now execute:

```text
BirthInput
  -> CalculationPolicy
  -> Manseryeok Adapter
  -> manseryeok 2.0.0
  -> CanonicalSajuSnapshot
```

Requirements remain:

- no upstream type leakage
- explicit engine/adapter/schema provenance
- unknown time must never become fabricated noon
- unsupported policy/timezone combinations fail closed
- calculation ambiguity must be preserved
- adapter behavior must be regression tested on golden and boundary fixtures
